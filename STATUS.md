# STATUS — where we are, at a glance

**Updated:** 2026-09-09 (**`Q99` RULED `all eight` — EVERYTHING AN ACTION FAMILY DECLARES IS PART OF ITS IDENTITY** ([R102](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-09-founder-ruling-R102.md)). The comparator that decides whether two action families contradict each other saw **four** of `ACTIONS.md` §2.2's **eight** declared keys and never `inputs`, `preconditions`, `reachability` or `payload_schema` — so two families differing only on `preconditions` collapsed with **nothing refusing**, a Haiku-tier actor recorded `applied`, and the survivor's ledger read **`n=0`**. That is the route by which `A3` still fires the governance stop criterion, and this ruling closes it **at the write door**. **It also DISSOLVES the read/write asymmetry** the founder was warned about: row 6f's read side already scored on all eight, so a narrow ruling would have left the two sides deliberately out of step. They now stand on one definition — the read scores agreement and never refuses, the write refuses a contradiction. **THE COST, WHICH IS THE POINT:** this is the most refusing option and collapses that succeed today will refuse tomorrow. **THE KNOWN WAY TO GET IT WRONG:** row 6d's own A3 fix compared the list-valued `effects` with `!=` and **CLOSED A LEGAL OPERATION** — non-overridably, at all three doors, under `force` — by refusing two families whose entries were merely in a different order. Four keys to eight adds **three more list-valued keys**, tripling that surface; row 6f already solved it on the read side with `_UNORDERED_DECLARED_KEYS` and **the write side must match**. **ROW 6g IS OPEN** — both its gates are now clear. **`A3` DOES NOT CLOSE UNTIL IT LANDS**, and the ACTIONS surface stays stopped for everything except that row. Not ruled and the row must RAISE rather than assume: whether every key deserves the same non-overridable severity, what to do when one side declares nothing, and **how many currently-legal collapses this refuses** — a measurement it owes. Previously: **ROW 6f HAS LANDED, NOT CLEAN, AND THE SUPERVISOR VERIFIED IT INDEPENDENTLY RATHER THAN COUNTERSIGNING THE ROW'S REPORT.** `R99` is implemented: `INTERFACE.md` §5.3 no longer promises `1.0` unconditionally, rules **5.3.2-9 to -16** land with **5.3.2-3 STRUCK rather than deleted** because it is what the registry promised for ten days, **one principle over two operands** (predicates by Jaccard of the written extents, action families by their governance declaration), **`min_confidence` governs the redirect path for the first time**, and **R99's authorisation to REFUSE is deliberately UNUSED** — an outcome the row pre-registered before it opened the resolver. Ten new ids `C3-20..C3-29`, all enumerated; **no new value in either closed vocabulary** (33 and 39 unchanged). **A3's DELIVERY IS REMOVED AND A3 IS NOT CLOSED:** the supervisor re-ran the row's probe and reproduced its table exactly — the walk that used to hand a Haiku-tier actor a **clean 1.0** now answers **`0.75`** with `identity_stale`, and the walk with no declaration answers **`None`** — while `retire` still returns RETIRED on ordinary calls and that actor still records `applied` with the survivor's ledger at **`n=0`**. **The write doors are the next row and the ACTIONS-surface stop stays in force.** Supervisor's independent check: all **four** gates exit 0, `6F-RUN.md` indexed, tree clean, on `origin`, beacon pin still an ancestor. Suites **932/311/0 sync** and **969/311/0 async** against floors of 912/301 and 949/301. **WHY NOT CLEAN:** round 1 reported nine findings using a mutation verifier the row LATER PROVED BROKEN — it mutated two helpers and credited the kill to the wrong one — so **nine-then-seven compares a bad number to a good one**; what survives is that round 2, measured with a working instrument, found **three BLOCKINGs in code four lenses had already passed**, two of them falsifying the row's own published reasoning by the same error twice. The row named that **method** failure — *twice generalising from a single measurement and publishing the conclusion* — as its worst, ahead of any defect, because a defect is fixed once and a method keeps paying out. Previously today: **ROW 6f's SPEC LANDED, AND THE FOUNDER'S RULING TURNED OUT CHEAPER THAN IT WAS ADVERTISED.** `INTERFACE.md` §5.3 **stops promising `1.0` unconditionally** and says exactly what it promises instead (`f388cd1`) — amended **before** the resolver was touched, verified by the supervisor: it is the only `INTERFACE.md` commit in the row and `registry.py` has none. All four sub-questions R99 left open are answered **in spec**: **score, NEVER refuse** (`5.3.2-11`) — **R99's authorisation to refuse is deliberately UNUSED**, an outcome the row **PRE-REGISTERED before opening the resolver** and which cost it the interesting answer; a stale redirect carries **`min(resolver_score, Jaccard)`** (`5.3.2-9`), derived rather than invented; **`kind="action"` is verified by its OWN operand** (`5.3.2-10`), since an action family has no extent and its identity stands on its governance declaration; and **`min_confidence` now governs this path** (`5.3.2-12`) — **[Observed] it did not**, a caller passing `min_confidence=2.0` was still handed `existing` at `1.0`. So **no caller is refused and no closed-vocabulary value is minted**: R99 §3's cost estimate was too high in the founder's favour. The resolver is still uncommitted and the adversarial rounds have not run — none of this is a claim about shipped behaviour. Previously today: **ROW 6f WENT TO VERIFY `A3` AND FOUND THE REGISTER'S ACCOUNT OF IT WAS STALE — THEN FOUND THE HARM REPRODUCES ANYWAY, BY A ROUTE NOBODY WAS COMPARING.** The governance register said in the present tense that A3's collapse doors let the harm through; commit `304967a` (*"A3 CLOSED"*) had fixed exactly those doors **2026-09-05, two days BEFORE the register was opened**, and the entry recorded row 6d's **round-1** observation as current. The supervisor then wrote R100's clause table from that entry on 09-09 **without re-running it**. Both records are **corrected in place** rather than edited away, per the register's own standing rule 4. **The `arm` ruling STANDS:** A3's harm reproduces at `HEAD` — verified by the supervisor re-running the probe itself, not by adopting the row's report — because **`_GOVERNANCE_KEYS` compares FOUR keys while `ACTIONS.md` §2.2 declares EIGHT**; two families agreeing on the four and differing on `preconditions` collapse with no refusal, `resolve_type` answers the dead word at **1.0**, a Haiku-tier actor records `applied`, and the survivor's ledger reads **`n=0`**. Governance register stays at **ONE** — one harm by a second route is not a second harm. New founder question **`Q99`**: which of the eight declared keys are governance? Row 6f also **CONFIRMED `P1`** (`registry.py`'s staleness check returns `False` unless BOTH sides are `kind="predicate"`, so the shipped detection is structurally blind to A3's shape) and **FALSIFIED its own `T2` discriminator by the test it fixed before looking** — deriving the read's policy from the write door would have made `resolve_type` refuse every merged word forever. Previously today: **THE FOUNDER RULED ALL THREE OPEN QUESTIONS, AND THE PROJECT'S OLDEST ONE IS CLOSED.** **`Q56` → `read`** ([R99](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-09-founder-ruling-R99.md)): an identity claim is verified **where it is MADE**. `resolve_type` may now answer **below 1.0** and may **refuse** when the claim does not re-establish at the read — the *expensive half* of Q56, which changes `INTERFACE.md` §5.3's shipped guarantee and was always the founder's. The cheap half shipped in row 4d on 2026-08-30 and could see staleness but not act on it. **This removes statement `E`**, the one design defect row 6e's audit found and the mechanism delivering the harm in **twenty-one of the twenty-three** kill-row trips — so **the kill row does NOT fire, and the fifteenth `stop` is RESOLVED, explicitly NOT declined a sixteenth time**: its subject is being removed rather than tolerated. Count stays **TWENTY-THREE**; a ruling is not a trip. **`Q97` → `arm`** ([R100](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-09-founder-ruling-R100.md)): the governance register's stop criterion is **LIVE**, and **entry 1 (`A3`) FIRES IT IMMEDIATELY** — disclosed before the word was given, so the halt is deliberate. **In force: no further ACTIONS-surface row launches until `A3`'s write doors refuse or warn**, and because the criterion's stated harm is that *the produced record reads as authorised*, any `applied` written through that path is suspect until established otherwise. **The two rulings meet on A3 from opposite ends** — R99 removes its read-side **delivery**, R100 halts the surface until its **write doors** are fixed; **A3 closes only when both land**, and it is still open. Governance register stays at **ONE** entry; firing adds none. **Item 4 → `drop it`** ([R101](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-09-founder-ruling-R101.md)), **and nothing was deleted**: the confirm-before-removing step found `r3lens_de7fdace` does not exist in either database on `oo-pg` — a container with `RestartCount=0` and a persistent volume since 2026-08-28 — and the name appears nowhere in `6D-RUN.md`. It was carried on the founder's page for four days without ever being observed. The real residue is **625 `oo_*` schemas**, which is a different object he did not authorise dropping, so it goes back to him as its own item with a default of *leave*. Previously: 2026-09-07 (**THE REGISTER AUDIT ANSWERED, AND IT FOUND ONE DESIGN DEFECT.** Row **6e** — opened by the founder under **R96** with the word *"audit"* — read all twenty-three kill-row trips **as a series**, which no row had ever done: every trip had been ruled *"implementation defect, not design"* by the supervisor who found it, one at a time. Its verdict is **neither** of the two answers the row was opened to choose between: **four recurring implementation families (18 of 23), one order-dependence singleton, and ONE DESIGN DEFECT — statement `E` — which is the mechanism that DELIVERS the harm in TWENTY-ONE of the twenty-three.** `E`: *the registry treats a fact checked at WRITE time as true at READ time, at `resolve_type`'s §5.3 guarantee of 1.0.* Every other family is a story about a **guard**; but a failed guard is only a trip when something then **answers at 1.0**, and that delivery is `E`. **The register counted the guards and never counted the delivery** — because every countersignature tested *design* with one narrow question, *is `namespace` untouched*, which appears **ten** times and which **R90** had already found to be two claims with one never tested. Its falsifier was fixed at **eight** before a single trip was read and it scored **eighteen**, so the finding survived a threshold it could not move. **`E` is not a discovery: it is `Q56`**, flagged to the founder as the sixth trip's root cause and **named the class-closing question nine times without a ruling** — and `INTERFACE.md` §5.3 already says refusing or answering below 1.0 *"would change the guarantee this section makes… It is Q56, it is the founder's, and it is open."* **`stop` is HELD OPEN, not declined a sixteenth time**; the recommendation to the founder is to rule `Q56` and let that ruling decide whether `stop` fires. Also today: **R97** gives governance identity its **own register** (entry 1 = `A3`, BLOCKING and open) rather than a place in the count, and **R98** **keeps** `C12-09`'s narrowing. The audit also answered a question about the register itself: **the kill criterion widened TWICE and only the second was written down** — so **TWENTY-THREE means two things, 2 under the written criterion and 21 under the widened one**. **LANDED 2026-09-07** — the six commits that carry all of the above are on `origin/main`, which had been stuck at `655f515` all day behind a **403** (`gh`'s active account lacked write access to this repo; resolved by re-authenticating as the owning account). The beacon pin `802ddf02` is **[Observed]** still an ancestor and the whole day's diff is **docs-only** — no storage contract was touched. Kill-row count stays **TWENTY-THREE**; an audit of the register does not add to it) (updated by the supervisor at every landing, and — this once — at a finding that could not land). Previously: 2026-09-05 (**row 6d LANDED NOT CLEAN — the identity-surface row the founder opened under R89, and the kill row is at TWENTY-THREE trips**: three adversarial rounds, the cap, none clean, findings **19 → 21 → 30**; nine trips countersigned, the twenty-third being `reinstate` — the fourth door that makes a word answer at 1.0 and the one the rule was never wired to ([R94](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-05-6d-supervisor-ruling-R94.md), [R95](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-05-6d-supervisor-ruling-R95.md)). **The provenance INVERTED**: round 2's routed constructions all pre-dated the commits beside them, while ~20 of round 3's 30 findings are defects this row INTRODUCED. One round-1 grade **withdrawn** (T3's harm at `reinstate`, graded before the value existed). `stop` put for the fifteenth time and with the founder as **OO-6**; nothing further opens without the founder) (updated by the supervisor at every landing). Previously: 2026-09-04 (**row 7a landed — Phase 3 has a specification, R78 CONFIRMED, and the kill row is STILL at FOURTEEN trips**: the row reached the criterion's shape eight times before any code exists and recorded all eight as `I-n` instance-surface records rather than incrementing the count. `stop` put for the eleventh time and the **first without a trip**) (updated by the supervisor at every landing). Detail: [ROADMAP.md](ROADMAP.md) · every doc: [docs/README.md](docs/README.md).

| Roadmap item | Status |
|---|---|
| Phase 0 discovery (internal; records not in the public repo) | ✅ closed 2026-08-28 — 0.2/0.2b/0.4 by assumption A1–A3 ([decisions](docs/decisions/2026-08-28-assumptions-in-lieu-of-office-answers.md)) |
| #1 `docs/specs/INTERFACE.md` v0 | ✅ done |
| #2 `docs/specs/PACKAGE.md` v0 | ✅ done |
| #3 Phase 2A reference implementation | ✅ done — 229 tests green, both backends |
| 3b Async adapter | ✅ done — 267 tests green, both async backends; generated from the sync source, not forked ([docs/runs/3B-ASYNC.md](docs/runs/3B-ASYNC.md)) |
| Docs folder reorg | ✅ done — `docs/specs` `findings` `runs` `tools` + link checker |
| `docs/USE-CASES.md` — the three validation fixtures | ✅ done |
| 3c Use-case validation pass (UC3 + adversarial loop on INTERFACE/PACKAGE) | ✅ done — kill-criterion mechanism exercised and **held**; 7 contortions recorded, suite 109 → 124, **nine** code defects fixed; **18 review rounds, no clean pass** — closed on an escalation ([`docs/findings/3C-VALIDATION.md`](docs/findings/3C-VALIDATION.md)). **Q1–Q8 ruled as R6–R13** 2026-08-29 |
| 3d Upstream fixes from beacon 21.1 (R5 savepoint transactions, degraded reference leg, attribute projections, doc sync, R8, R12, R13) | ✅ done — **the transaction seam beacon 21.2 builds on**. Borrowed connections use SAVEPOINTs and never commit; a **third** reference leg that is natively degraded, not simulated; a per-leg coverage report (*a conformance claim without its coverage line is not a claim*); suite 124 → **129**, `340 passed` / `374 passed`. **Three adversarial rounds, six reviewers, no clean pass** — closed on the cap with a convergence note; **five BLOCKING** findings, every one of them a lying adapter that had been passing the suite ([`docs/runs/3D-RUN.md`](docs/runs/3D-RUN.md)) |
| #4 [`docs/specs/EDGES.md`](docs/specs/EDGES.md) v0 — typed relationships, incl. type-to-type `equivalent_to` (**R7**) | ✅ done — **the spec Tenshen slices 1–2 build against.** An edge family **is** a `kind="edge"` `TypeEntry`, so it inherits the whole proposal→approval loop and `consumers`; EDGES adds no call of its own (the surface went 13 → 14 in row 3e, for `reinstate`). `neighbors` capped at depth 2 (R13's consequence); three adapter primitives; `equivalent_to` symmetric, **non-transitive**, and **verified not to weaken the shipped `merge_types`**. Three design tests driven through **real** CMS / NYC / beacon data by five runnable probes ([`docs/tools/edges_*.py`](docs/tools/)) — every pre-registered CMS count reproduced. **Eleven contortions recorded, none designed away.** INTERFACE §5.12 **15 → 19**, §5.4 **11 → 16**, both per **R3**; `check_spec_drift.py` now checks enum *contents*, not just field names. **Three adversarial rounds, six reviewers, ten BLOCKING and ten MAJOR — no clean round**, closed on the brief's cap with a convergence note. **Q12–Q21 want a ruling** |
| 4c Edge semantics (R34 payload validation, **R38** `neighbors` follows the successor chain with `via_successor`, R39, R37 `amend_edge`, R40, `check_merge_guard.py`) | ✅ done 2026-08-30 — 196 → **226 ids**, sync **549** / async **584** on three legs, gates 4 → 5, warnings 25 → 29. **R38 is live: `merge_types` is now safe on a store with edges in it.** Three rounds, six reviewers, 19 BLOCKING, **did not converge** — honest note at [`4C-RUN.md`](docs/runs/4C-RUN.md) §6.6; every finding was in code the row had just touched. **Kill row tripped a fourth, fifth and SIXTH time in test** (fourth found by the new checker; fifth and sixth by the loop while that checker exited 0); the sixth is *different in kind* — stale is not equal — and its root cause is **Q56, founder-visible**. Q49–Q60 ruled as **R48–R57** ([`2026-08-30-4c-rulings-R48-R57.md`](docs/decisions/2026-08-30-4c-rulings-R48-R57.md)); Q50 + Q56 flagged with defaults |
| 4d Identity staleness (**Q56 default** warn at the read; **R54** `_extent`/`predicates`/`list_types` resolve the identity; **R55** write-door warnings; staleness axis in `check_merge_guard.py` first) | ✅ done 2026-08-30 — 226 → **249 ids**, sync **596** / async **631** on three legs, warnings 29 → **32**, `Refusal.reason` 28 → **30**, `check_merge_guard.py` 1 → **6 axes**, constraint 8's gate reaches `INTERFACE.md` (21 rules, 20 pinned). **Kill row tripped a SEVENTH and an EIGHTH time — one defect found twice: the guards compared alias BYTES, the resolver compared NORMALISED words.** Three rounds, six lenses, 31 findings, 16 BLOCKING, **did not converge** — note at [`4D-RUN.md`](docs/runs/4D-RUN.md) §6; **five findings were in row 4c's guards**, the first time the loop reached outside the current row. Q61–Q68 raised **Q61–Q68 ruled as R61–R66** ([`2026-08-30-4d-rulings-R61-R66.md`](docs/decisions/2026-08-30-4d-rulings-R61-R66.md)); Q65, Q66, Q68 carried to the founder's Q56 decision |
| 6b ACTIONS v0 build + R53 guard extraction | ✅ done 2026-08-30 — 249 → **327 ids**, sync **757** / async **796** on three legs (post-rename); primitives 18 → 21, store v5, `Refusal.reason` and `warnings` now 30/30 and 32/32 returned by code; `check_spec_drift.py` reads `ACTIONS.md`. **Kill row tripped a 9th, 10th and 11th time — one missing-operand defect three rounds apart, closed by the required-keyword rule**; loop did not converge ([`6B-RUN.md`](docs/runs/6B-RUN.md) §6). **Q69–Q78 ruled as R67–R76** ([`2026-09-01-6b-rulings-R67-R76.md`](docs/decisions/2026-09-01-6b-rulings-R67-R76.md)); Q70/Q71 founder-visible with defaults, routed to the Q40/R44 retention decision |
| 6c ACTIONS v0.1 amendments (**R70** namespace-scoped typo rule, **R71** warn on retired family, **R72** `ref_key` inverse, **R73** `review_invocation` + `unknown_invocation`, **R75** `retire(successor=)` alias design test) | ✅ done 2026-09-02 — 327 → **366 ids**, sync **837** / async **874** on three legs; `warnings` 32 → 37 values, `Refusal.reason` 30 → 31; `check_merge_guard.py` **7 → 10 axes**. **Two of the five changes were decided by a design test run BEFORE any patch**, and both times the test said something the reading had not. **Kill row tripped a 12th, 13th and 14th time — one per round** — and the 14th **predates this row**, found in doors no row of this lineage had touched. **Three rounds; the findings did not shrink and the row was STOPPED rather than finished** ([`6C-RUN.md`](docs/runs/6C-RUN.md) §6.7). **Q81–Q84 raised**; Q82 is an unconstructed kill-row suspicion carried forward |
| 4b **EDGES v0 implemented** (primitives 16—18, `neighbors`, edge conformance, **R31**) | ✅ done — **the read seam Tenshen slices 1—2 build against is shipped code now, not a specification.** Adapter primitives **15 → 18** on all three reference backends; store schema **3 → 4**; the four edge capability flags are ordinary members of `CAPABILITY_FLAGS`, so the matrix reaches them (15 configurations, all conformant). `equivalent_to` seeded, — which changed what a fresh store contains and moved thirteen census ids with it. Suite **150 → 194** (`C17` 34, `C18` 10), `480 passed` / `516 passed`, three legs `CONFORMANT`. **`C18` re-runs row #4's three design tests through the shipped store and every pre-registered number reproduces.** Ruling **R31** lands as standing constraint 8, enforced and watched failing four ways; `check_spec_drift.py` goes from two gates to **five** (EDGES' printed shapes and all eighteen primitive signatures were ungated, and both had drifted). Warnings **20 → 22**, both new values minted because running the code found cases the specification had not. **Three adversarial rounds, six reviewers, five NOT YET — eight BLOCKING, eight MAJOR**, closed on the cap: two rounds found a defect *inside the mechanism built to prevent that very defect*, and the third swapped in a **consumer lens** that produced five of its six findings. **Sixteen deviations recorded, none designed away** ([`docs/runs/4B-RUN.md`](docs/runs/4B-RUN.md)). **Q27—Q34 want a ruling** |
| 3e v0.1 amendments (**R6** cross-namespace lookup, **R10** name-level schemas, **R11**/**R19** `reinstate`, **R17** `created_by: derived`, **R21** `Provenance.source_version`) | ✅ done — **the surface goes 13 → 14 calls**, the first call added since v0 was written. `resolve_type(search_namespaces=)` closes contortion 8 — UC3's central finding, where the answer was decided by which namespace the caller picked *before asking*; name-level attribute schemas close `C15-07`, the limitation recorded in that mechanism's own flagship justification; `reinstate` gives a permanently burned word a specified way back, with three refusals including the twentieth and twenty-first `Refusal.reason`. Store schema **1 → 3** — one drop-and-recreate (§9.4's licence, spent once and deliberately) and one `ALTER`. Warnings **16 → 20**, and §5.4 is now **checked mechanically** like §5.12. Suite 129 → **150**, `388 passed` / `421 passed`, three legs `CONFORMANT`. **Three adversarial rounds, six reviewers, six NOT YET — ten BLOCKING and sixteen MAJOR**, closed on the brief's cap with a convergence note: **four of the ten BLOCKING were defects inside a previous round's own fix**, and **thirteen of the twenty-one new ids exist to pin claims the specs already made** ([`docs/runs/3E-RUN.md`](docs/runs/3E-RUN.md)). **Q22–Q26 want a ruling** |
| #6 [`docs/specs/ACTIONS.md`](docs/specs/ACTIONS.md) v0 — governed actions | ✅ done — **the third leg of `VISION.md` §12's pattern is specified**: typed entities (INTERFACE), typed relationships (EDGES), and now **governed actions**. An action family **is** a `kind="action"` `TypeEntry`, so it inherits the whole proposal→approval loop and adds **no call to `INTERFACE.md` §5**; preconditions closed at four kinds each answered by an existing call, effects closed at four operations with the six governance calls barred from being one, and **no executor and no scheduler** — §4 states out loud that the gate is advisory by construction. `Refusal.reason` **21 → 28**, warnings **22 → 25** (per **R3**), suite **194 → 196** ids, `606 passed`. Three design tests driven through real beacon/CMS/NYC data by **four** runnable probes ([`docs/tools/actions_*.py`](docs/tools/)), **96 checks**; the 128-tool ceiling re-measured rather than cited (`common 45 + task 48 + project 21 + person 13 = 127`). **Nine contortions recorded, none designed away.** **Three adversarial rounds, six reviewers, three KILL-ROW TRIPS — two of them against the shipped registry** (empty predicate extents comparing byte-identical, `C10-09`; `retire(successor=)` performing a merge with none of its guards, `C9-18`) — closed at the cap with a convergence note that says plainly it did **not** converge: findings halved twice and the first `READY` in six rows arrived, but every round found a kill-row route the previous fix had not closed. **Q35—Q48 want a ruling**, six founder-visible ([`docs/decisions/2026-08-29-6-questions-Q35-Q48.md`](docs/decisions/2026-08-29-6-questions-Q35-Q48.md)) |
| #5 Phase 2B Tenshen migration (beacon) | ⏳ beacon 21.1 spec landed (`6e87d61a`, Draft, founder approval pending); 21.2 build should target 3d for the transaction seam |
| #7 Phase 3 ingestion / mapping | ⬜ not started — **its three must-decides are ruled ahead of it, R58–R60** (paging with one `known` rule, tenant-blind protocol + two-tenant fixture in 2B, one three-valued `Condition` language) ([`2026-08-30-phase3-decisions-R58-R60.md`](docs/decisions/2026-08-30-phase3-decisions-R58-R60.md)); opening the row is the founder's (Q48) |
| 7a [`docs/specs/INGEST.md`](docs/specs/INGEST.md) v0 — the Layer B / Phase 3 ingestion-mapping SPEC row | ✅ done 2026-09-04 — **Phase 3 has a specification, and R78 is CONFIRMED**: the host holds the instances, this project defines resolution over them through **two read primitives**, and after a walk-through that scanned 14,627 host rows the shipped registry held `[('entity','facility')]` and no instance row. `resolve_instance` is a **new** call with **five** outcomes — the fifth, `unknowable`, pre-registered as a prediction and then forced by construction. **90 rules, 5 design tests, 128 checks** over real CMS and NYC data. **Three adversarial rounds, eleven lens verdicts, no clean round; findings 37 → 47 → 51 and BLOCKING 11 → 19 → 21.** The kill row's shape was reached **eight times before any code exists** — the `I-1`…`I-8` **instance-surface records**, *one question asked at eight doors* — and **the trip count stayed at FOURTEEN**, unincremented across nine constructions, because none reached a shipped door. **Standing rule (e)** minted; rule (d) sharpened (R84), made countable (R85), extended (R87) and complemented (R88). **`stop` put for the eleventh time and the FIRST without a trip**, with the supervisor recommending *continue, but stop housing the loop in build rows*. **Q85–Q93 open, Q91 the founder's; eight open findings named in §8.1** ([`7A-RUN.md`](docs/runs/7A-RUN.md)) |
| 6d identity-surface row (**R89**; rulings **R90–R95**) — the guards that decide when two words name one thing | ✅ landed NOT CLEAN 2026-09-05 — **ids 366 → 399**, `check_merge_guard.py` **10 → 18 axes**, `Refusal.reason` 31 → 33, warnings 37 → 39; three legs **1861 passed / 602 skipped / 0 failed**. **The kill row goes 14 → 23.** Three rounds, the cap, **no clean round**, findings **19 → 21 → 30**. Round 3's result is the row's own: **~20 of 30 findings are defects this row introduced**, one of them a false refusal that closed a legal operation for two rounds. A round-1 grade **withdrawn** and re-scored. Open and named: X1, X2, X5, X7–X11, §5.10's printed refusal order, and the keyed-probe residual. **Q94/Q95/Q96 + R95's across-kinds question are the founder's; `stop` is OO-6** ([6D-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/6D-RUN.md)) |
| 6e **the register audit** (**R96**) — are the 23 trips 23 implementation defects, or ONE design defect found 23 times? | ✅ **ANSWERED, LANDED, CLOSED — and its question is now RULED.** `Q56` → **`read`** on 2026-09-09 ([R99](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-09-founder-ruling-R99.md)), which removes the one design defect this row found and resolves the kill row's fifteenth `stop` without a sixteenth decline. The row's own work is complete; the change it authorises is a new row. The row's record is on `origin` and indexed in `docs/README.md` (`e51d16f`) — the documented gate it had not yet passed when its analysis landed, caught by running the close gates rather than assuming a landed row is a finished one. The session was closed 2026-09-07 on supervisor authority with the full trail. Wrote **no product code**, by design. **I=5 · R=17 · D=1**; `R+D`=**18** against a falsifier fixed at **8** before any trip was read. Pre-registration (§0) is provably the **first and only** commit touching its record, which is what makes that falsifier binding. The one `D` is **`E`**, the read-time delivery of every guard failure, and it is **`Q56`** — the founder's, named class-closing **nine** times and never ruled. Verdict on the routed construction (warn-on-write): **VERIFIED CONSTRUCTIBLE**, by code-read only — no probe, no suite. Record: [`6E-RUN.md`](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/6E-RUN.md) |
| Phase 4 generalise | ⬜ not started |

Legend: ✅ done · 🔵 in flight · ⏳ queued · 🟡 assumed, pending evidence · ⬜ not started · 🔴 blocked
