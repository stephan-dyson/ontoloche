# ontoloche — document index

Every document, organised by what it is for. Links are GitHub links to `main`. Newest state first: the [roadmap](https://github.com/stephan-dyson/ontoloche/blob/main/ROADMAP.md) says what is done and what is next; the [decisions](#decisions) say why.

## Strategy — read to argue with the venture

| Doc | What it is |
|---|---|
| [VISION.md](https://github.com/stephan-dyson/ontoloche/blob/main/VISION.md) | The thesis, what was observed, what is assumed, what is not validated. Claims tagged [Observed] / [Inferred] / [Assumed]. |
| [STATUS.md](https://github.com/stephan-dyson/ontoloche/blob/main/STATUS.md) | **Where we are, at a glance** — one row per roadmap item, one status each. Updated at every landing. |
| [ROADMAP.md](https://github.com/stephan-dyson/ontoloche/blob/main/ROADMAP.md) | Phases 0–4, kill criteria, standing constraints, and the **"Ordering for the Tenshen rebuild"** table — the live execution order. |
| [USE-CASES.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/USE-CASES.md) | **The three validation fixtures** every spec and design is tested against — Tenshen (single-writer registry), CMS citations (flat government export), NYC Open Data (many agencies, colliding words) — plus the validation protocol and Wikidata as governance precedent. |
| [WALKTHROUGH.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/WALKTHROUGH.md) | The product, concretely: a non-technical analyst goes from a spreadsheet to a sent action in five steps. |

## Specs — the contracts being built against

| Doc | Status | What it is |
|---|---|---|
| [INTERFACE.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/specs/INTERFACE.md) | v0, unstable | The type-registry contract: **fourteen** calls around a proposal→approval loop; `consumers`, `predicates`, lifecycle (`retire` **and** `reinstate`, **R11**); cross-namespace lookup in `resolve_type` (**R6**); the closed **twenty**-value `Refusal.reason` vocabulary (§5.12); Tenshen and CMS design tests. |
| [PACKAGE.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/specs/PACKAGE.md) | v0, unstable | The `ontoloche` package: a fifteen-primitive storage-adapter protocol, SQLite + Postgres backends, `attributes` schema-per-kind, and the **145-test contract suite that defines conformance** (the Phase 2B gate), plus ruling R12's per-leg coverage report. |
| [ACTIONS.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/specs/ACTIONS.md) | v0, unstable | **Typed, gated actions over the registry and the edge store.** An action family **is** a `kind="action"` `TypeEntry` — the third instance of EDGES' pattern (a family is a row of the vocabulary; its instances live in a store beside the registry) — so it inherits propose→approve, `resolve_type`, lifecycle and `consumers` unchanged and adds **no call to `INTERFACE.md` §5**. Preconditions are closed at four kinds, each answered by a call that already exists; effects are closed at four operations, and the six governance calls may **never** be one — *an action may propose; only a human, or an auto-policy a deployment set deliberately, may approve*. **No executor and no scheduler**: §4 says out loud that the gate is advisory by construction and names the two things that make it not nothing. `Refusal.reason` **21 → 28**, warnings **22 → 25**, per **R3**. The 128-tool ceiling is a first-class design input, re-measured rather than cited. **Three adversarial rounds, six reviewers, three kill-row trips — TWO of them against the shipped registry, both fixed and pinned (`C10-09`, `C9-18`)** — closed at the cap with a convergence note that says plainly it did **not** converge. **Nine contortions recorded, none designed away; Q35—Q48 want a ruling.** |
| [INGEST.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/specs/INGEST.md) | v0, unstable | **The mapping layer between ETL and the ontology — Phase 3's first row, and a SPEC row that ships no product code.** **R78 CONFIRMED:** the host holds the instances and this project defines resolution over them through **two READ primitives** — there is no `put`, and *two primitives is the strongest available evidence the seam holds*, because a protocol needing a third would be an instance store with a different name. `resolve_instance` is a NEW call (**R77**, never an extension of `resolve_type`) with **five** outcomes, not four: the fifth, `unknowable`, was **pre-registered as a prediction before the probe existed** and then forced by construction — a truncated scan under a four-outcome set answers `proposal` for a facility that exists in the same table. **Ninety rules** (`C20-01`…`C20-90`), two capability flags, a fourth reference shape (`CandidateRef`, which has no `id` and never acquires one), and a **twelve-term three-valued `Condition`** whose Kleene composition is R60's one change, landed across INTERFACE/EDGES/ACTIONS in the same commit. **Five design tests, 128 checks, every one over real CMS and NYC data** — 419,479 CMS rows / 14,627 CCNs / 104 shared names, and a live 9.7M-row NYPD partition. **Three adversarial rounds, eleven lens verdicts, NOT ONE CLEAN ROUND — 51 findings in round 3 against 47 and 37, and BLOCKING went 11 → 19 → 21.** The kill row's shape was reached **eight times before any code exists**, recorded as the `I-1`…`I-8` **instance-surface records** (R83–R87) — *one question asked at eight doors* — and **the trip count stayed at FOURTEEN throughout**, because none was constructed against a shipped door. **Standing rule (e)** is this row's: *the extent an identity is decided over, and the facts that govern the decision, are the same at every door that reads, writes, keys, gates or counts it.* **Twelve contortions recorded, eight open findings named in §8.1 rather than hidden; Q85–Q93 want a ruling, Q91 the founder's.** |
| [EDGES.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/specs/EDGES.md) | v0, unstable | **Typed relationships over the registry.** An edge family **is** a `kind="edge"` `TypeEntry` — not a fifth kind, not a predicate — so it inherits propose→approve, `resolve_type`, lifecycle and `consumers` unchanged, and EDGES adds no call of its own (the surface went 13 → 14 in row 3e, for `reinstate`). `equivalent_to` (**R7**) says *the same thing, kept apart* without licensing a merge; `neighbors(node, families, depth)` is capped at depth 2 (R13's consequence) with Rule K/U throughout; three adapter primitives. Three design tests driven through real CMS/NYC/beacon data; **eleven contortions recorded**; ten questions (Q12–Q21) open. |

## Findings — evidence, in the order it was produced

| Doc | Roadmap row | Headline |
|---|---|---|
| [3C-VALIDATION.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/findings/3C-VALIDATION.md) | 3c | **UC3 (NYC Open Data) run against INTERFACE v0 and PACKAGE v0.** Three agencies, one word, three unrelated meanings (`uvpi-gqnh`, `erm2-nwe9`, `693u-uax6`). Scoping and the `cross_namespace_merge` refusal hold; **five INTERFACE contortions (8–12) and two PACKAGE ones (B7–B8)** recorded, and two contract tests added (109 → 111) for a cross-namespace gap the suite never asserted. |

## Run records — what the code actually does

| Doc | Row | Result |
|---|---|---|
| [2A-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/2A-RUN.md) | #3 | **229 passed, 0 failed, 0 skipped** in one run — SQLite 113 + Postgres 16.14 113 + 3 backend-independent; CMS design test reproduces every pre-registered count; fourteen recorded deviations. |
| [3D-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/3D-RUN.md) | 3d | **340 passed / 374 passed, three reference legs in one run each** — the two full backends plus `sqlite_minimal`, a real SQLite store with four of the nine tables absent (beacon finding **U2**). Carries U1's savepoint-scoped `transaction()` over a borrowed connection (ruling **R5** — the seam beacon 21.2 builds on), U3's `attribute_projections`, U4's doc sync plus a PACKAGE drift check, **R8**, **R12**'s per-leg coverage report and **R13**. Suite 124 → **129** ids. **Three adversarial rounds, six reviewers, six NOT YET verdicts** — closed at the brief's cap with a convergence note (§5.1), five BLOCKING and eight MAJOR findings fixed, every one of them reproduced by running the system rather than reading it. |
| [4B-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/4B-RUN.md) | 4b | **480 passed / 516 passed, three reference legs, 150 → 194 ids.** `EDGES.md` v0 **implemented** — the read seam Tenshen slices 1—2 build against is shipped code now, not a specification. Adapter primitives **15 → 18**, four edge capability flags, store schema **3 → 4** (`oo_edge`, plus the `oo_event.edge_id` amendment row #4 claimed twice and landed nowhere), `add_edge` / `retract_edge` / `neighbors` / `edge_provenance`, `equivalent_to` seeded. **Ten of `C17`'s ids hold a BLOCKING finding row #4's own loop had already fixed — in a throwaway probe kit the package does not import**; `C18` re-runs its three design tests through the shipped store and every pre-registered number reproduces (CMS 400/69/400 over 92 tags; NYC 102 edges, 18 of 25 matched, max 16 trees on one lot). Ruling **R31** lands as standing constraint 8, and the drift checker goes from two gates to **five**. **Three adversarial rounds, six reviewers, five NOT YET — eight BLOCKING and eight MAJOR**, closed on the cap with a convergence note that says plainly the findings did **not** shrink: what changed each round was where the reviewers were pointed. **Sixteen deviations recorded, none designed away. Q27—Q34 want a ruling** |
| [4C-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/4C-RUN.md) | 4c | **549 passed / 584 passed, three reference legs, 196 → 226 ids.** Edge *semantics*: `payload_schema` validated (**R34** — after its specified key was **[Observed]** to make a governed family unregisterable, D-4c-1), `amend_edge` taken (**R37**) only after passing the design test its ruling made a condition, a second retraction refused (**R39**), and **`neighbors` follows the successor chain (R38, founder-visible)** — an edge endpoint reference resolves to the identity it now belongs to, which is what makes `merge_types` safe on a store with edges in it. `propose_type(kind="predicate")` never auto-approves (**R40**). **The row's largest artefact is a checker, not a feature:** [`check_merge_guard.py`](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/check_merge_guard.py) enumerates every extent state and — from `registry.py`'s own AST — every caller that changes what a name resolves to, and **found the kill row's fourth trip on its first run**. **`ROADMAP.md`'s kill row then tripped a fifth and a SIXTH time, both found by the adversarial loop while that checker exited 0** — the sixth is the first that is *different in kind*: the guard looked correctly and then the fact changed (Rule U's fourth operand, **stale is not equal**). `Refusal.reason` did **not** grow, four times over. **Three rounds, six reviewers, 19 BLOCKING — the loop did NOT converge and §6.6 says so; not one finding across three rounds was in code this row had not touched.** **Eleven deviations recorded, none designed away. Q49—Q60 want a ruling** |
| [6C-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/6C-RUN.md) | 6c | **366 ids, sync 837 / async 874, three legs.** ACTIONS v0.1 amendments — **R70** (rule 10-9 narrowed to the `namespace`-scoped pool), **R71** (a retired blast-radius family warns at both doors), **R72** (`parse_ref`, the public inverse of `ref_key`), **R73** (`review_invocation` specified, `unknown_invocation` minted as the thirty-first `Refusal.reason`) and **R75**, whose alias half was decided by a **design test run before any patch**: the test found TWO alias shapes and each verdict was true of one of them, so the missing re-point was the defect and the guard's reading was right. **The kill row tripped THREE times — once per round, the twelfth, thirteenth and fourteenth** — and the last two were proved by BISECT: the fourteenth **predates this row entirely**, in three doors no row of this lineage had touched, found by a lens briefed to look elsewhere. `warnings` 32 → **37** values; `check_merge_guard.py` **7 → 10 axes**, every new one mutation-proved. **Three rounds, seven lenses; the findings did NOT shrink (13, 12, 12) and every round produced a trip — the row was STOPPED rather than finished** (§6.7), and **every round-3 finding is inside a fix this row had already made**. Six deviations; **Q81–Q84** raised |
| [7A-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/7A-RUN.md) | 7a | **A spec row's loop, run before the build row — and the first row in this register to close with `stop` put and NO trip.** Five design tests pre-registered and run (**36 + 13 + 17 + 11 + 41 = 128 checks**); `INGEST.md` v0 at ninety rules. **Three rounds, four lenses in each of the last two, no clean round, and the findings did NOT shrink: 37 → 47 → 51.** Round 2's fix auditor found **five BLOCKING inside round 1's own two commits**; round 3's found four more inside round 2's one. **Seven failures of standing rule (d) in round 2 alone, every one inside the fixes of the round that cited rule (d) as its reason for existing** — which is why R85 moved the rule onto the **commit** and R88 added its complement: *a fix commit lists what it declined, or a later round finds it and counts it* (**nineteen** bound no commit here). **§6.11's seven predictions were committed BEFORE any lens returned and scored seven of seven** — and §6.16a says the half that matters: *prediction is not prevention; P3 was written by the author of rule 3-19 and shipped anyway*, a correction R88 adopted **against its own R87**. The row also caught **its own mutation harness reporting a real defect as surviving** (§6.10e-i), and round 3 caught the *recorded method* being the superseded one. Closed at the cap with a convergence note that says plainly it did not converge. **Q91–Q93 raised; eight open findings named as the build row's declared inputs.** |
| [6B-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/6B-RUN.md) | 6b | **327 ids, sync 757 / async 796, three legs.** ACTIONS v0 BUILT (families, `preflight`, the invocation ledger, primitives 19–21, store v5, all 58 planned `C19` ids + the loop's 18 more) AND ruling R53's guard extraction with R64's naming. **Kill row tripped a NINTH, TENTH and ELEVENTH time — one defect three rounds apart** (the missing-operand family, closed by the required-keyword rule); three rounds, five lenses, **did not converge** (§6). Q69–Q78 ruled as **R67–R76**. |
| [2026-08-30-RENAME-ontoloche.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/2026-08-30-RENAME-ontoloche.md) | — | **The project is renamed ontoloche** ("on-tuh-LOH-chee", the *-che* after Apache; founder ruling 2026-08-30). One `git mv` + 75-file mechanical rewrite, validated in a worktree first; unasync regeneration wrote 0 of 25 files (byte-identical); historical records carry a note, not a rewrite. Post-rename: sync **747 passed**, async **785 passed**, three legs. |
| [4D-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/4D-RUN.md) | 4d | **596 passed / 631 passed, three reference legs, 226 → 249 ids.** **Identity staleness** — the class the kill row's sixth trip named. The **Q56 default** ships: `resolve_type` re-reads both predicate extents when an exact hit is answered through an alias or a successor and, if they no longer agree, returns the redirect **unchanged at confidence 1.0** carrying `identity_stale`. *The fact is reported, never suppressed; refusing or lowering the confidence is the founder's half and stays open.* Ruling **R54** makes `_extent`, `predicates()` and `list_types(predicate=)` resolve the **identity** rather than the written word — while every collapsing guard keeps the written-word reading, because resolving identities there would make a merge its own justification. Ruling **R55** announces the same fact at both write doors. `check_merge_guard.py` goes from **one axis to six** (+ staleness, spelling, one-word-one-identity, forward-declared successor, the NAME door), and constraint 8's rule gate reaches `INTERFACE.md` for the first time — four subsections, **21 rules, 20 pinned**. **The kill row tripped a SEVENTH and an EIGHTH time, and they are one defect found twice**: the guards compared alias BYTES while the resolver compared NORMALISED words, so `aliases: ["Commentable"]` was written where `["commentable"]` is refused non-overridably — and after `identity_key` was published to close it, the one door it did not reach (§5.4's *a retired name is not reusable*) reached the same 1.0 in four ordinary calls. **Three rounds, six lenses, 31 findings, 16 BLOCKING — the loop did NOT converge**, and for the first time **five findings were in a PREVIOUS row's guards**, which is what 6b's extraction row now exists to review. Round 3's second lens ran **21 mutations** against this row's own rule tables and found **two rows decorative** — an id could be deleted with all 245 green. **Nine deviations, Q61—Q68 raised** (Q66 and Q68 founder-adjacent) |
| [3E-RUN.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/runs/3E-RUN.md) | 3e | **388 passed / 421 passed, three reference legs, 129 → 150 ids.** The v0.1 amendments: cross-namespace lookup in `resolve_type` (**R6**, closing contortion 8 — UC3's central finding), name-level attribute schemas (**R10**, closing `C15-07`), **`reinstate`** as the fourteenth call (**R11**/**R19**), `created_by: derived` (**R17**) and `Provenance.source_version` (**R21**). Store schema **1 → 3**, one drop-and-recreate and one `ALTER`. `Refusal.reason` **19 → 21**, warnings **16 → 20** and checked mechanically for the first time. **Three adversarial rounds, six reviewers, six NOT YET — ten BLOCKING and sixteen MAJOR**, closed at the brief's cap with a convergence note (§5.1). Eight of the twenty-one new ids came from the rulings; **thirteen came from the loop**. **Q22–Q26 want a ruling** |

## Decisions — assumptions and rulings, each with what would revise it

| Doc | What it settles |
|---|---|
| [2026-09-04-founder-ruling-R89.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-founder-ruling-R89.md) | **R89 (founder-ruled, verbatim "agree with your rec"):** `stop` is **not** taken — the eleventh decline and the first resting on a row with no trip behind it. **The loop moves out of build rows** into a **dedicated identity-surface row (6d)**: no feature, no amendment, lens set = the fourteen trip records **and** the eight `I-n` cells, surface = the type-identity guards and the five doors against the ten-axis checker, and **round 1 is pre-registered prediction across the whole surface before any lens runs** — the only configuration in which a loop that can predict its next defect gets to prevent one. Row 7a validated as landed. The `INGEST` build row is **not** opened; E22 and E2 stay its first obligations. Next ruling R90; next question Q94. |
| [2026-09-04-7a-supervisor-ruling-R88.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-7a-supervisor-ruling-R88.md) | **R88:** countersigns row 7a's honest convergence note — three rounds, eleven lens verdicts, **no clean round**, findings **37 → 47 → 51**. Puts the `stop` option for the **eleventh** time and the **first without a trip**, so the question becomes *is the loop worth what it costs?* rather than *has the guard failed?* **Recommendation: continue, and STOP HOUSING THE LOOP IN BUILD ROWS** — open the dedicated identity-surface row, because *a loop that can predict where its next defect will be is wasted in a row that writes the rule first and predicts second*, and only a row with no diff of its own turns prediction into prevention. Carries a **correction to R87**, made by the worker and adopted: predicting your own next defect and shipping it is not a success. Accepts §6.16c into the register: **a fix commit lists what it DECLINED, or a later round finds it and counts it.** Approves landing `INGEST.md` as a **v0 with eight named open findings** carried in §8.1. Count stays **fourteen**. Next ruling R89; next question Q94. |
| [2026-09-04-7a-supervisor-ruling-R87.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-7a-supervisor-ruling-R87.md) | **R87:** confirms **`I-8` — mis-directed**, an eighth cell: the closure is walked forward only while the shipped `_identity_closure` §3.4a cites as normative walks **both** ways and consults aliases (**[Observed]** `backward` and `alias` are **zero** in `INGEST.md`, `alias` zero in the kit). A cell, not a construction inside `I-2` — that is depth, this is **direction** — and it is the **ordinary** post-retirement path ending in **`mode='auto'`, a second row written with no human**. **A normative citation binds you to ALL of it**: R84's clause failed inside the section written to satisfy it, so a citation must enumerate by name what it adopts and what it declines. And the register gains a new kind of fact — round 3's predictions were pre-registered before any lens ran and **P3 is confirmed**, so the loop is now **predictive**, not merely productive; the `stop` question is owed that beside the non-shrink. Count stays **fourteen**. Next ruling R88; next question Q91. |
| [2026-09-04-7a-supervisor-ruling-R86.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-7a-supervisor-ruling-R86.md) | **R86:** settles what the `I-n` series counts — **a record is a CELL of the table, not a construction**, so round 2's nine constructions do not become nine records. B1, B2 and B5 fall inside `I-4`, `I-5`+`I-1` and `I-3`. **D3 does not fit and earns `I-7` — mis-governed**: the chain walk succeeds and the extent is right, but the successor's `MatchPolicy` and `Condition` are swapped in under a caller who named the predecessor, defeating rule 5-6 (*two entries may declare different policies; two callers may not*) with **one caller getting both**, reaching **R59's stated reversal condition** by retiring a type, and changing **73 of 1,373** real CMS resolutions. Rule 5-7 is verified to have **no carrier**. **Standing rule (e) is amended before shipping** to cover the governed facts as well as the set. Also corrects R85 on the record: its table was built on six records and is **extended**, not retroactively credited. Next ruling R87; next question Q91. |
| [2026-09-04-7a-supervisor-ruling-R85.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-7a-supervisor-ruling-R85.md) | **R85:** countersigns round 2's four kill-row-family records as **`I-3`–`I-6`** (the write door unbound by the successor chain; the act keying on the raw label while the gate decides on `norm`; a drained-but-unwritten proposal invisible to the guard; a candidate page deduping two host rows onto one `instance_id`). Count stays at **fourteen**. **The six instance-surface records are ONE TABLE** — one question at six doors, the set wrong because it was truncated / mis-walked / mis-written / mis-keyed / mis-timed / mis-counted — so they are closed as **one change**, not one fix per round, which is the fourteenth countersignature's lesson about trips 8/12/13/14. Proposes **standing rule (e)**: the extent an identity is decided over is the same set at every door that reads, writes, keys, gates or counts it, and a door that cannot prove it answers `unknowable`. **Standing rule (d) failed seven times in one round while being cited by name**, so it gains a countable form — every commit that mints a rule carries the enumeration of the doors it binds. Next ruling R86; next question Q91. |
| [2026-09-04-7a-supervisor-ruling-R84.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-7a-supervisor-ruling-R84.md) | **R84:** countersigns **`I-2`** — round 2's fix-auditor found rule 3-14 (minted in round 1 to close K6, and stating the identity read follows the successor **chain** under R38) implemented as a `while` ending in an unconditional `break`, so two ordinary `retire(successor=)` passes answer one type identity twice at 1.0. Not a fifteenth kill-row trip and the count stays at **fourteen** — on firmer ground than the record claimed, because **the shipped surface is the control here and it passes**: `registry.py:1403` closed this class in row 4d round 2 and the spec re-opened it by restating R38 instead of citing the implementation. **Standing rule (d) gains a second clause: the enumeration crosses the document boundary** — a rule minted in a spec must name the shipped callers it binds, and cite a shipped implementation as its normative reference. The fix must also close the rider defect in the same four lines (the eighth trip's shape). Next ruling R85; next question Q91. |
| [2026-09-04-7a-supervisor-ruling-R83.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-04-7a-supervisor-ruling-R83.md) | **R83:** row 7a's round-1 defect — a truncated candidate scan that FINDS a match answering `existing` at 1.0 on a label twelve CMS facilities answer to — is **not** a fifteenth kill-row trip; the trip count stays at **fourteen**, because the register counts states constructed against shipped code at a shipped door and row 7a ships none. It is recorded instead as **`I-1`**, first of a separate `I-n` instance-surface series that never merges with the trip count. Verified: it is the **fifth trip verbatim** one identity surface down (*partial is not equal*, the `why` discarded); standing rules (c) and (d) are confirmed to bind the instance surface; the loop reached it **before the diff existed**, the cheapest finding in the register's history and an argument about *where* the adversarial loop runs. `stop` is **not** put an eleventh time (it attaches to trips only); Q56 stays the class-closing question and the founder's. The recommended dedicated identity-surface row gains an instance half. Next ruling R84; next question Q91. |
| [2026-09-03-6c-rulings-R79-R82.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-03-6c-rulings-R79-R82.md) | R79–R82: row 6c's four questions ruled after its landing at fourteen kill-row trips. The flat-form namespace constraint binds where the word is WRITTEN, as a warning first and a refusal one row later (R79); a guard's escape may not depend on page order, and an unconstructable kill-row state is pinned as a test that proves the doors refuse it rather than carried as a suspicion (R80); no fourth round — the cap stands, and constraint 7 gains the clause that a row whose every round tripped hands its §6.7 predictions to the next identity row (R81); `word_previously_retired`'s cost is the host's and its visibility half is closed into Q80 (R82). Recommends a **dedicated identity-surface row** as the kill row's next home, behind the Layer B spec row. Next question: Q85. |
| [2026-09-02-phase3-repoint-R77-R78.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-02-phase3-repoint-R77-R78.md) | The **Phase 3 re-point** (founder directive, 2026-09-02): the convergence claim — the design partner's missing middle **is** this project's ingestion wedge — **confirmed** against `VISION.md` §4b/§5 and the roadmap's own homing of instance resolution, and the roadmap's only reason for sequencing Phase 3 later (the pollution machine before the filter) shown **met**. **R77 (founder-ruled):** instance resolution is Phase 3's and `resolve_type` is never extended to cover it — closes INTERFACE §11's open item. **R78 (supervisor, [Assumed], falsifiable by the spec row's first design test):** the host holds the instances; this project defines the resolution protocol over them through adapter primitives and stores no instance rows. The dependency-ordered **critical path** (§5), with the correction that Phase 3's decisions were already taken as R58–R60 — the next row is the **Layer B spec row**, blocked on a slot, not a decision. One founder ask: extend the Rule of the ordering to Phase 3 (§6). |
| [2026-09-01-6b-rulings-R67-R76.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-09-01-6b-rulings-R67-R76.md) | R67–R76: 6b's ten questions — the alias door keeps its `indexes_membership=False` skip (R67), the ledger's retention evidence routed to the founder's Q40/R44 decision with defaults (**R68, R69 — both founder-visible**), five amendments become **row 6c** (narrowed typo rule R70, warn-on-retired-family R71, the `ref_key` inverse R72, `review_invocation` specified + `unknown_invocation` minted R73, the `retire(successor=)` alias design test R75), a prose contradiction named as constraint 8's limit (R74), and the entity-collapse gap recorded as a `Capabilities` question on the U3 precedent (R76). Suite floor now 327 / 757 / 796; the kill row's `stop` option put again at eleven trips. |
| [2026-08-28-assumptions-in-lieu-of-office-answers.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-28-assumptions-in-lieu-of-office-answers.md) | A1–A4: assumed answers to the Phase 0 partner questions. **A5 (confirmed by the founder):** Tenshen will be rebuilt on top of ontoloche; it may depend on the package once Phase 2A passes contract tests on CMS data. |
| [2026-08-29-supervisor-handoff.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/handoffs/2026-08-29-supervisor-handoff.md) | Supervisor handoff at the 2026-08-29 power-down: 4c at 10/12 with round 1's fix uncommitted on disk, the fifth kill-row route, pre-ruled Q49–Q55, the cycle-cron recipe. |
| [2026-08-30-4d-rulings-R61-R66.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-30-4d-rulings-R61-R66.md) | R61–R66: 4d's eight questions — one `identity_stale` value (R61), the closure cache measured not ruled (R62), 6b names the guards' reading with R53's extraction (R64), permanence of `identity_stale` stays and is **evidence for the warn-only half of Q56** (R65); **Q65 (a third option for Q56), Q66 and Q68 (non-Latin words) carried to the founder with defaults**; suite floor now 249 / 596 / 631. |
| [2026-08-30-phase3-decisions-R58-R60.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-30-phase3-decisions-R58-R60.md) | R58–R60, **all founder-visible**, ruled ahead of Phase 3: the façade **pages** (`list_types`, `neighbors`, the ledger) under one rule — `known` counts the page, `complete` is about the set, a cursor is the third field, and a guard never reads a page (R58); the protocol stays **tenant-blind** and 2B's harness carries a two-tenant fixture (R59); **one three-valued `Condition` language** for gates, preconditions, tenancy and the ledger, specified before it is built (R60). |
| [2026-08-30-4c-rulings-R48-R57.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-30-4c-rulings-R48-R57.md) | R48–R57: ten of 4c's twelve questions ruled; three create **row 4d — identity staleness** (`_extent` resolves the identity, write-door warnings, a staleness axis in the checker first); **Q50 and Q56 flagged to the founder with defaults** (Q56 = verify an identity claim where it is MADE — the sixth kill-row trip's root cause; default: warn at the read, confidence stays 1.0). |
| [2026-08-29-6-rulings-R40-R47.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-6-rulings-R40-R47.md) | R40–R47: eight of #6's fourteen questions ruled (predicate proposals never auto-approve — R40, row 4c; value-level + ledger conditions are one Phase 3 decision — R41; the rest recorded); six product-level questions flagged to the founder with defaults. |
| [2026-08-29-6-questions-Q35-Q48.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-6-questions-Q35-Q48.md) | **Q35–Q48, open for a ruling** — row #6's fourteen. Six are founder-visible: whether the compliance arm sells enforcement or evidence (**Q39**), whether §10 should be cut (**Q47**), whether `VISION.md` §7 names a first deliverable (**Q48**), the third surface wanting a value-level condition (**Q35**), a precondition over the ledger (**Q44**), and whether **R24**'s tenancy ruling survives actions (**Q38**). Plus the standing direction round 3's founder lens gave: **#6 should be the last spec row before #5**. |
| [2026-08-29-4b-rulings-R32-R39.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-4b-rulings-R32-R39.md) | R32–R39: 4b's eight questions — four become row 4c (edge payload validation, `neighbors` follows the successor chain with `via_successor` — R38, founder-visible — retraction refusal, `edge_amended`); the rest recorded. |
| [2026-08-29-3e-rulings-R27-R31.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-3e-rulings-R27-R31.md) | R27–R31: 3e's five questions — `complete` stays honest-but-rare, schema delete at v1, census linear for v0, and **R31: every numbered spec rule ships executable** (standing constraint 8). |
| [2026-08-29-edges-rulings-R17-R26.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-edges-rulings-R17-R26.md) | R17–R26: EDGES v0's ten questions — `created_by: derived` (R17), one cross-field rule (R18), `reinstate` covers families not edges (R19), `model_tier` on edge provenance / no tier gate (R20), `source_version` (R21), tenancy stays the host's job (R24), the 9.7M-degree node routes paging to Phase 3 (R25). |
| [2026-08-29-3d-rulings-R14-R16.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-3d-rulings-R14-R16.md) | R14 the 2B gate requires a BorrowedHarness (declarations verified, not declared); R15 beacon 21.2's real run is the fourth loop round; R16 `MergeResult.warnings` populated, doc follows code. |
| [2026-08-29-3c-rulings-R6-R12.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-3c-rulings-R6-R12.md) | R6–R12: the seven questions row 3c raised, each ruled yes/no with its row; and the kill-row trip on Tenshen's capability shape, recorded and judged an implementation defect (founder may overrule). |
| [2026-08-29-2b-upstream-findings-R5.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-29-2b-upstream-findings-R5.md) | **R5:** an adapter over a host-owned session may implement `transaction()` as a SAVEPOINT, declared via `transaction_scope`; dispositions of the five findings beacon 21.1 routed upstream (U1–U4 → row 3d; U5 already fixed). |
| [2026-08-28-package-v0-rulings.md](https://github.com/stephan-dyson/ontoloche/blob/main/docs/decisions/2026-08-28-package-v0-rulings.md) | R1 async adapter is roadmap row 3b — **landed 2026-08-28, and its "mirroring is mechanical" assumption held**; R2 `attribute_census` stays package-local; R3 `Refusal.reason` is a closed vocabulary. |

## Tools and data

| File | Use |
|---|---|
| [make_sample.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/make_sample.py) | Cuts the 400-row Montana sample from the public CMS citations file. |
| [characterize.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/characterize.py) | Counts the pathologies over all 419,479 rows (the numbers in 0.5). |
| [check_links.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/check_links.py) | Every relative markdown link in the repo resolves. Run before landing. |
| [check_spec_drift.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/check_spec_drift.py) | **Every data shape and signature printed in `INTERFACE.md` still matches the code.** Written at row 3c after six review rounds each found one that did not; it found two more immediately. The contract suite runs it. |
| [check_capability_matrix.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/check_capability_matrix.py) | **Every optional capability, declined one at a time, still conforms.** PACKAGE §3.2 claimed that and was wrong for six of eight flags for four deliverables. The contract suite runs it. |
| [edges_probe_kit.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/edges_probe_kit.py) | A **throwaway** in-memory implementation of `EDGES.md` v0, deliberately outside the package. Row #4 ships no edge store, and its design tests are *executed* rather than argued. Imports the closed `REFUSAL_REASONS` from the package so a probe cannot widen it. |
| [edges_cms_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/edges_cms_probe.py) | UC2: the implicit edges in the 400-row CMS sample, every count checked against the frozen ground truth (400 / 69 / 400 over 92 tags). |
| [edges_nyc_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/edges_nyc_probe.py) | UC3: live Socrata. Three scoped `borough` types joined by `equivalent_to`, and the **shipped** `Registry` then asked to merge them anyway — refused, twice. |
| [edges_beacon_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/edges_beacon_probe.py) | UC1: beacon's three relationship shapes, parsed **read-only**, mapped column by column onto `EdgeRecord`. Counts six `created_by`-shaped vocabularies. |
| [actions_probe_kit.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/actions_probe_kit.py) | A **throwaway** in-memory implementation of `ACTIONS.md` v0, outside the package. Imports the closed `REFUSAL_REASONS` and `WARNING_VALUES` so a probe cannot widen a vocabulary, and imports `edges_probe_kit` rather than re-implementing edges — so an `edge_exists` precondition is answered by the same `neighbors` row #4 wrote. |
| [actions_beacon_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/actions_beacon_probe.py) | UC1: beacon's 222 action modules, parsed **read-only**, three of them expressed as families. Re-derives the 128 cap, the 127 budget and `common 45 + task 48 + project 21 + person 13`; pins beacon at a commit and prints what has drifted since. |
| [actions_cms_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/actions_cms_probe.py) | UC2: `flag_facility_for_review` over the 400-row CMS sample. The severity precondition is **not expressible**, and `EDGES.md` §2.4.1 closes the one detour at both layers. |
| [actions_nyc_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/actions_nyc_probe.py) | UC3: `reconcile_borough`. An action cannot manufacture the transitivity `equivalent_to` refuses, and the **shipped** `merge_types` still refuses `cross_namespace_merge` twice. |
| [actions_governance_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/actions_governance_probe.py) | The governance machinery no fixture walks — every kill-row door, the approval gate at both layers, the gate-to-record window, the ledger reads §4 rests on. **Both adversarial rounds found their sharpest defects here**, which is why it exists. |
| [edges_capability_probe.py](https://github.com/stephan-dyson/ontoloche/blob/main/docs/tools/edges_capability_probe.py) | The operational machinery **executed** rather than asserted — retraction, every declined capability, the dead-end walk, the assembly bound, both enforcement layers of §2.4.1. Added in round 1 of the adversarial loop; grew in rounds 2 and 3, each time because the unrun corner was where the defect was. |

**Standing constraint 0 applies to everything here: no employer data, ever — public CMS data only.**
