# Rulings 2026-08-29 — the three questions row 3d left (Q9–Q11 → R14–R16)

Supervisor rulings under the founder's make-assumptions directive. Source: [`docs/runs/3D-RUN.md`](../runs/3D-RUN.md) §7 and the 3d landing summary. Each is yes/no on the worker's recommendation; the founder may reverse any.

## R14 (Q9) — a savepoint declaration without a `BorrowedHarness` stays `CONFORMANT, DECLARATIONS UNVERIFIED` in general; **for the Phase 2B gate it is not enough**

**General conformance:** as implemented. Refusing an honest adapter that has no borrowed mode would punish honesty; the run names the unverified claim, which is Rule U applied to conformance itself.
**The A5 gate specifically:** beacon's `work_link_types` adapter **must supply a `BorrowedHarness`** so `transaction_scope="savepoint"` and `owns_schema=False` are *verified*, not declared — because the whole point of R5/U1 is that seam, and *"a conformance claim without its coverage line is not a claim"* (R12) applies with equal force to an unverified declaration. Recorded here; relayed to beacon with the 3d facts.

## R15 (Q10) — no fourth synthetic round in this row; **beacon 21.2's real run is the fourth round**

The loop's own convergence note says the defect class narrowed from "the mechanism does not exist" to "one edge of the mechanism is wrong." The next signal with real information is a real consumer over a real borrowed session — beacon 21.2 with its harness (R14) — not a fourth fresh reader of the same prose. Two safeguards: (1) 21.2's harness results are routed back upstream as findings, the way 21.1's were; (2) row 3e's loop re-covers PACKAGE §3/§6 when it amends the surface. Residual risk stays stated in 3D-RUN §5.1: *an adapter whose declarations nobody can check* — now visible, not eliminated.

## R16 (Q11) — `MergeResult.warnings` is populated; the doc correction stands

3c's round 7 made every merge carry warnings (Rule K/U information about what the merge could not know). That was the right change — a merge that cannot compute extents must say so on the result, not only in a refusal. The former "reserved and always empty in v0" text was the stale half; the worker's correction (doc follows code) is confirmed.
