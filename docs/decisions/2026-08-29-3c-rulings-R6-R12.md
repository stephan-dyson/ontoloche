# Rulings 2026-08-29 — the seven questions row 3c left (Q1–Q7 → R6–R12), and the kill-row trip

Supervisor rulings under the founder's make-assumptions directive. Source: [`docs/findings/3C-VALIDATION.md`](../findings/3C-VALIDATION.md) §6 — five decisions from the UC3 walk-through and two from the adversarial loop, each with a recommendation so a ruling is yes/no. The founder may reverse any of them; the two with product weight (R8, R12) are flagged.

**Sequencing principle applied throughout:** the founder's ordering rule is *unblock the Tenshen rebuild first*. Anything Tenshen's 2B needs goes to row 3d (in flight next). Type-to-type relations go to #4 EDGES. Surface additions that serve UC3/Phase 3 and governance but block nothing Tenshen needs go to a new row **3e — v0.1 amendments**, after EDGES. `v0` is labelled unstable precisely so that additive amendments are cheap; "wait for v1" is not required when the change is additive and default-off.

## R6 (Q1) — cross-namespace lookup in `resolve_type`: YES, additive, row 3e

`search_namespaces: Sequence[str] | None = None` on `resolve_type`; hits land in `alternatives` as `("<namespace>:<name>", score)`; `Resolution.complete` is `true` only when the caller named every namespace. No v0 caller changes. **Why not defer to v1:** scoping without lookup reintroduces mechanism 2 (re-propose what you cannot find) as the answer to mechanism 4 — UC3 is the kill-criterion fixture and this is its central finding. The Rule-K mitigation already taken (`scoped_to`, `why_incomplete`) stays.

## R7 (Q2) — `equivalent_to` between scoped types: YES, it is an EDGE — assigned to #4 EDGES.md

Accepted as stated: a relation between two scoped types is a type-to-type edge, so **EDGES v0 must carry type-level edges, not only instance-level ones**; `equivalent_to` is its first named family (symmetric, non-merging, provenance-bearing). A field on `TypeEntry` is rejected for the reason given. Added to the #4 brief.

## R8 (Q3) — value-level consumer gates: NEITHER option in v0; the warning IS taken (row 3d). *Founder-visible.*

`consumers()` warns when `gate` names no registered predicate, so `would_drop` is never read as a fact about a live gate. Option (1), `gate_values`, is deferred to Phase 3's decision — it makes the registry know what a *value* is, which INTERFACE §2.1 refuses on purpose; that refusal is a product boundary and the founder should see it before it moves. Option (2) is rejected (it deletes mechanism-C visibility, C11-02).

## R9 (Q4) — resolver-dependent tests non-binding for foreign resolvers: CONFIRMED as applied

The applied fix (skip keyed on the resolver being replaced, `resolver_factory` / `--resolver`, reference backends still run all three) is confirmed. Widening `_PROJECTION_FAMILIES` to coordinates stays forbidden — fitting the table to the second dataset. Moving domain-semantic judgement fully behind `Resolver` is a v1 item, recorded.

## R10 (Q5) — attribute schemas keyed per name as an override: YES, row 3e

`(namespace, kind, name)` schemas shadow the per-kind schema. It changes `oo_attr_schema`'s key; §9.4 already permits dropping a v0 store, so the migration is a drop-and-recreate with a version bump. Not v1 — it is the pollution the mechanism exists to prevent (§5.1), observed on CMS's own two value sets.

## R11 (Q6) — `reinstate`: YES, specify and implement, row 3e

`reinstate(type, reason, *, reinstated_by, namespace) -> TypeEntry | Refusal`, refusing when the retirement's `successor` is itself active (`successor_active` — sixteenth `Refusal.reason`, added to INTERFACE §5.12 in the same change per R3). A permanently burned word, retireable by one actor for everyone, is a governance defect in both UC1 (runtime classifier) and UC3 (dozens of publishers). The §5.9 justification correction already taken stands.

## R12 (Q7) — conformance for a multi-flag-degraded backend: the two-flag rule STANDS; a coverage report is REQUIRED, row 3d. *Founder-visible.*

No new non-negotiable flags. Every conformance run prints which contract ids it could not exercise and why (`CONFORMANCE` summary), and **a conformance claim without its coverage line is not a claim.** This is the same move as `ConsumerReport.complete=False`. It changes what "conformant" asserts — from "the whole suite passed" to "the whole suite passed; N ids not exercisable on this backend, listed" — which is the honest reading A5's gate needs. The founder should know that *"Tenshen conforms as a third backend"* is currently verified for one declined capability and assumed for the rest; 3d's natively-degraded leg (U2) plus this report closes that gap.

---

## The kill-row trip — recorded, judged not a stop

ROADMAP kill table: *"A capability predicate gets merged as a duplicate → Stop — this is the failure that destroys meaning."*

**What happened [Observed, commit `0e89037`, 3c round 9]:** on Tenshen's declared capability shape (`indexes_membership=False`), every predicate extent came back empty, two predicates with genuinely different members compared **equal**, the non-overridable `predicate_merge` refusal never fired, and the merge fell through to an overridable guard. Reproduced before being believed. Fixed: an extent that could not be computed is not a byte-identical extent (Rule U); the refusal is now non-overridable with a reason saying the extents were unknowable; C9-08 pins the compound shape.

**Judgment:** this is an implementation defect in the guard, caught by the adversarial loop before any real merge, on a fixture — not the *design* flattening a true distinction. The kill row exists for the latter. The row stays armed; the trip is evidence that it is the right row and that constraint 7's loop is load-bearing. **The founder may read it the other way** — that a guard which silently failed on the very backend it was written for is a design-level failure — and if so the stop is his to call. Recorded in the ROADMAP kill table with this commit.

## R13 (Q8, raised after the file above was ruled) — the facade stays unbounded in v0, and says so

**The finding [Observed, 3C-VALIDATION s6 Q8]:** adapters implement keyset pagination (now tested by C0-10), but no call site in either facade passes `limit` or `after`; `list_types(namespace=None)` is a full fetch at UC3 scale.

**Ruling:** accept the recommendation. PACKAGE s3.3 states plainly that the registry does not page in v0 and why (Rule K has no answer yet for whether `known` counts the page or the set); pagination is an adapter capability the product path does not use. The `TypeListing` paging design is Phase 3's decision, because the ingestion loop is the consumer that would force it. Row 3d carries the one-paragraph doc statement; no code.

## The kill-row trip — SECOND instance (2026-08-29 16:45, #6 round 2, commit fcb05b3)

**What happened [Observed, commit body]:** `merge_types` guard 2 refused unless the two predicate extents were byte-identical — and **two EMPTY extents are byte-identical**. Reproduced end to end against the shipped registry: two predicates proposed by an `ai:` actor at Haiku into an auto-approving namespace went live, then merged under two acknowledgements. Row 3c had closed the *unknowable*-extent end of the same expression (C9-08); nothing pinned the *empty* end. Fixed in `registry.py`, pinned by C10-09 (a NON-empty identical extent still merges — the guard is narrowed, not banned); INTERFACE §2.3/§5.10 amended; suite 194 → 195 ids, 604 passed.

**Supervisor judgment, restated with the new evidence:** still an implementation defect caught by the loop before any real merge — but it is the **second hole in the same guard expression in 24 hours**, and the pattern is now nameable: *a comparison-based guard treats "no evidence" as "equal evidence."* The fix makes the guard demand positive, non-empty, identical evidence before a predicate merge, which is the design the kill row wants. The row stays armed. **This is the founder's call to overrule:** two trips on one row could reasonably be read as the design (extent comparison) being the wrong shape, not two bugs. Escalated in the 16:55 pane report.

## The kill-row trip — THIRD instance (2026-08-29 17:30, #6 round 3, commit 05b8e04)

**What happened [Observed, commit body]:** not `merge_types` this time. `retire(successor=)` redirects `resolve_type` to the successor at confidence 1.0 (INTERFACE s5.3 calls that a guarantee), so it *is* the collapse the merge guards exist to refuse -- and it carried none of them. The pair that `merge_types` refuses non-overridably under all five acknowledgements collapsed through retirement with no refusal, no acknowledgement and no warning, across kinds too. Reproduced against the shipped `Registry`. Fixed by giving `retire` s5.10's two identity guards (#2 and #3), non-overridable and `force=True` included -- force overrides what could be SEEN, never what would become TRUE. Pinned by C9-18 (a plain retirement still works; so does one whose successor shares a non-empty identical extent). Suite 195 -> 196 ids, 606 passed. Recorded in the ROADMAP kill row by the worker in the same commit.

**Supervisor judgment:** the worker's diagnosis is accepted and it widens the second trip's, rather than repeating it -- the defect is **a guard written for ONE CALL over a fact that MORE THAN ONE CALL can change**, on top of the two-valued-over-three-valued comparison. Still not the design the kill row guards against: `namespace` is untouched across all three trips and `cross_namespace_merge` still refuses on live NYC data; what failed three times is the *guard implementation*, in two expressions and now two callers. The row stays armed. **The fix owed is a checker, not a fourth patch:** row 4c's `check_merge_guard.py` must enumerate the **callers** (`merge_types`, `retire(successor=)`, and any other call that changes what a name resolves to) as well as the four extent states, and fail on any caller that lacks the identity guards. Row 4c's item 6 is amended accordingly.

**Escalation restated:** three trips on one row in one day is the strongest evidence yet for the founder's `stop` option; my recommendation is still to continue *conditional on* 4c shipping the checker, because each trip was caught in test by the loop, never in a real merge, and each fix narrowed toward the design the row wants. The founder's word overrules.

## The kill-row trip — FOURTH instance (2026-08-29, row 4c, found by `check_merge_guard.py` itself)

**What happened [Observed, row 4c].** Not `merge_types`, and not `retire` either. Row #6 round 3's ruling was that *"the fix owed is a checker, not a fourth patch"*, and that row 4c's [`check_merge_guard.py`](../tools/check_merge_guard.py) must enumerate the **callers** as well as the four extent states. Its Part A does that from `registry.py`'s own AST — every function that writes a `successor` or an `aliases` onto a stored record, the two fields that change what a name resolves to — and it named one nobody had guarded on its first run: **`import_types`**.

The probe reproduced the collapse end to end against the shipped `Registry`:

1. `commentable` and `searchable` — two live predicates whose extents are non-empty and genuinely different (`{note}` and `{doc}`);
2. `merge_types` refuses that pair **non-overridably**, under every acknowledgement the call accepts;
3. `commentable` is retired — an ordinary, permitted governance act, **no successor**;
4. `import_types` writes `aliases: ["commentable"]` onto `searchable` with **no refusal, no warning and no acknowledgement**;
5. `resolve_type("commentable")` goes from `proposal / None / 0.4762` to **`existing / searchable / 1.0`** — a confidence `INTERFACE.md` §5.3 calls a registry **guarantee** — while the two extents stay different.

**Why `alias_collision` did not see it.** That guard refuses an alias that is a **live** entry's name, because §5.9b minted it to stop *two active entries holding one word between them*. A retired name is not a live entry — but **a retired predicate name still resolves, and a retired predicate still has an extent.** The guard was written for a *collision*; the failure is a *collapse*. Same write, different question.

**Fixed** by giving the alias write §5.10's identity guards #2 and #3, non-overridable, refused through `import_types`' existing `import_refused:<reason>` shape (it returns entries, not `Refusal`s). Pinned by **`C12-08`**, and by **`C12-09`** for the half a careless fix deletes: two predicates with non-empty *identical* extents may still be aliased, because `C10-09` narrowed the guard rather than closing the operation.

**A fifth instance of the same class, found beside it and fixed in the same commit.** `retire`'s overridable consumer guards ran *before* its non-overridable identity guards, so on `indexes_membership=False` a predicate retirement-with-successor was refused `no_consumer_evidence` — which advertises `force=True` — when the true answer was `predicate_merge`, which never moves. The outcome was safe; the story was wrong, and the story is what a caller acts on. Row 3c fixed the identical defect in `merge_types` (moving `cannot_record_override` after the four non-overridable refusals) and the lesson had not been carried across. Pinned by **`C9-19`**.

**Supervisor judgment, restated.** The diagnosis widens a third time and this is its full form: **a guard written for ONE CALL, over a fact that MORE THAN ONE CALL can change, reached through MORE THAN ONE FIELD.** `successor` and `aliases` both re-point what a name resolves to; three calls write them. The design is still not what tripped — `namespace` is untouched across all four, and `cross_namespace_merge` still refuses on live NYC data.

**What is different about the fourth trip, and it is the whole argument for continuing:** the three before it were found by human adversarial reviewers, one round at a time, each after the fix for the last one shipped. **This one was found by a checker, before any reviewer read the row, and that checker now runs inside the contract suite on every commit** — Part A fails on a caller it has never heard of, whether that caller is guarded or not, so the *next* one cannot arrive unnoticed. That is precisely what the third trip's ruling asked for, and it is the first time this class has had a mechanical gate. The stop option remains the founder's.

**Supervisor countersignature (2026-08-29 20:25, commit `8e641e6`).** The fourth-trip record and the judgment paragraph above were drafted by row 4c's worker in this file; the supervisor read them against the commit and concurs without amendment. What changes the recommendation's basis: the first mechanical gate on this class now runs in the contract suite on every commit (`check_merge_guard.py`, Parts A and B), and it found its own first trip before any reviewer read the row. Recommendation to the founder unchanged -- continue; the `stop` option remains the founder's.


## The kill-row trip — FIFTH instance (2026-08-29, row 4c's first adversarial round)

**What happened [Observed, row 4c].** `_extent` read **one page** of a predicate's members and returned it. Every guard that compares two extents — `merge_types`' refusal #2, `retire(successor=)`'s (added by the third trip) and `_alias_identity_breach`'s (added by the fourth) — took `set(self._extent(...)[0])` and **threw away the third element**, which is the `why` saying the read was partial. So **two predicates whose FIRST PAGE of members happened to match compared equal**, and all three callers performed the collapse the same store refuses non-overridably when it is not paging.

Reproduced end to end against the shipped `Registry` on `DegradedAdapter(page_cap=2, page_cursor=True)` — this repository's own honest-paging double, added by row 3e's third round with the note that *"PACKAGE.md §3.3 permits it and UC3's scale produces it"*. True extents `{aaa_doc, bbb_note, zzz_draft}` and `{aaa_doc, bbb_note}`: `merge_types` returned a `MergeResult`, and `resolve_type("commentable")` answered `searchable` at confidence 1.0.

**This is Rule U's third operand on one expression.** Row 3c closed *unknowable is not equal*. Row #6's second round closed *empty is not equal*. **Nobody had closed *partial is not equal*** — and the read path had been publishing exactly that fact, as `PredicateEntry.why_extent_incomplete`, since row 3c. The guards discarded the one signal the read emits.

**Fixed on two axes, because there are two backends and one fix does not cover the other:**

| the backend | what it is | the defence |
|---|---|---|
| an honest **page** — `complete=False` **with** a cursor | there IS a rest | `_extent` loops to exhaustion |
| a **truncated** answer — `complete=False`, **no** cursor, or a page it cannot count, or a cursor it repeats | there is no rest to read | the guards fold `_extent`'s `why` into `knowable` |

Pinned by **`C10-11`** on both, on all three callers, plus the narrowing case (a genuine duplicate read across two pages still merges). `check_merge_guard.py` gains a `partial` and a `truncated` state for every collapsing caller — **its three real legs all answer in one page, which is precisely why it exited 0 while this was live**, and that gap is now closed by two synthetic rows rather than by hoping a future leg pages.

**Supervisor judgment.** The shape of the diagnosis does not change; its reach does. After five trips it reads: **one guard, one expression, three operands (unknowable / empty / partial), three callers (`merge_types`, `retire`, `import_types`), two fields (`successor`, `aliases`).** The design is still not what tripped — `namespace` is untouched across all five and `cross_namespace_merge` still refuses on live NYC data.

**What the fifth trip says about the fourth's argument.** Row 4c argued that the checker was the answer because it found the fourth trip mechanically. The fifth trip qualifies that honestly: **the checker exited 0 while this was live**, because its legs do not page. A checker only asks the questions its fixtures can pose, and the adversarial loop is what found the question it was not asking. Both are load-bearing and neither replaces the other — which is the strongest available argument for keeping constraint 7's loop, and against reading the fourth trip as evidence that reviewers are now optional.

**Supervisor countersignature (2026-08-29 22:10, commits `3a1d38b` and `9935823`).** The fifth-trip record and the judgment above were drafted by row 4c's worker in this file; the supervisor read them against both commits and concurs without amendment. Two things the supervisor adds for the register. First, the recommendation's basis moves again: after the fourth trip it was *"the checker now runs on every commit"*; after the fifth it is *"the checker AND the loop, because the checker exited 0 while this was live"* -- constraint 7's loop is not made optional by constraint 8's gate, and the fifth trip is the evidence. Second, the fifth trip is the first one whose cause is a **read-path** property (paging) rather than a guard expression, which makes paging a correctness question for the Phase 3 decision on R25, not a Phase 3 nicety; recorded here so that ruling cites it. Suite floor restated correctly: row #6's floor was 196 ids and `484 passed, 122 skipped` (606 **collected**); this row stands at 220 ids, `537 passed, 142 skipped` sync and `572 passed, 142 skipped` async, so the floor held. Recommendation to the founder unchanged -- continue; the `stop` option remains the founder's.


## The kill-row trip — SIXTH instance (2026-08-30, row 4c's third adversarial round), and it is different in kind

**What happened [Observed, row 4c].** Four independent doors, all reproduced end to end against the shipped `Registry`, on both reference backends and on the async mirror, with the full suite green and all five gates exiting 0.

**Door 1 — two ordinary merges, nothing else.** `merge_types`' guard #2 compares `left`'s extent to `right`'s. The same write re-points **`left.aliases`** at `right`, and **nothing compares those.** So:

1. `commentable`, `searchable`, `taggable` all have extent `{note}`;
2. `merge(commentable → searchable)` — legal, extents identical. `searchable.aliases = ("commentable",)`;
3. a new type `doc` declares `searchable` and `taggable` — ordinary vocabulary growth. Now `searchable` and `taggable` are `{doc, note}` and `commentable` is still `{note}`;
4. `merge(searchable → taggable)` — legal, *those two* extents are identical. `commentable`'s alias rides across, uncompared;
5. `resolve_type("commentable")` → **`taggable` at confidence 1.0**, on a pair `merge_types` refuses **non-overridably** when asked directly.

**Doors 2–4.** `reinstate` re-activating a row whose dormant aliases nothing re-checked; `import_types` skipping **both** alias guards for a `status="deprecated"` row, which `reinstate` then makes live; and an alias written while the word was still free, with the predicate created afterwards — `propose_type` matched `name` and never `aliases`, so two live entries came to hold one word, which is `C16-06`'s own whole-store invariant failing on a store two ordinary calls build.

**Why this trip is different from the five before it.** Trips 1–5 were all one sentence: *the guard did not look properly* — at an unknowable extent, at an empty one, at all (a caller with no guard), through a different field (the alias door), at a partial page. **This one is: the guard looked correctly, and then the fact changed.** Every identity guard here compares extents at **write** time; `resolve_type` grants confidence 1.0 at **read** time; and four things move in between — a row is created under the aliased word, a row's `status` flips, an extent grows, an alias is transferred by a later merge. The claim was *true when it was made*.

> **Rule U's fourth operand.** Unknowable is not equal (trip 1). Empty is not equal (trip 2). Partial is not equal (trip 5). And **STALE is not equal** (trip 6).

**Fixed** at all four doors: `merge_types` validates the aliases it transfers; `reinstate` runs the identity guards over the aliases it re-activates; `import_types` runs them whatever the row's `status` is; `propose_type` refuses a name a live entry already answers to. Pinned by **`C10-13`**. `check_merge_guard.py` gains **`status`** as a third identity field — narrowly scanned, because the word is ubiquitous — and its `reinstate` verdict is corrected from `collapses=False` to `True`, which is the caller enumeration working exactly as designed: **a person's judgement, written down and wrong, is one a reviewer can find.**

**A fifth defect of the same family, found beside them.** `INTERFACE.md` §5.9 said the guards that transfer to `retire(successor=)` are *"the two that are about IDENTITY rather than about evidence — §5.10's refusals #2 and #3"*, filing `different_consumer_sets` under *evidence*. §5.10's own rationale for #1 is *"merging asserts that every consumer of one accepts the other"* — an identity claim — and its table marks it *"No. Not by `force`, not by `acknowledge`."* **[Observed]** a pair refused under all seven acknowledgements, collapsed by `retire(successor=, force=True)`. Two sections disagreed about which bucket #1 was in, and the disagreement had a `force` door in it. Transferred and pinned by **`C9-20`**.

**Supervisor judgment, and it should be read alongside the founder's `stop` option.** Six trips. The design is *still* not what tripped — `namespace` is untouched across all six and `cross_namespace_merge` still refuses on live NYC data — and every trip was caught in test, none in a real merge. But the honest counterweight to the fourth trip's optimism is now on the record twice: the checker exited 0 through trips **five** and **six**, because its fixtures could not pose the questions. **The class is not closed by these fixes.** What closes it is a decision this row did not have the standing to make: see `4C-RUN.md`'s **Q56** — whether an identity claim should be verified where it is *made* (at `resolve_type`'s 1.0) rather than only where it is *written*. That is a change to a shipped guarantee and it belongs to the founder.

**Supervisor countersignature (2026-08-30 00:30, commit `a3f9e6e`).** The sixth-trip record and the judgment above were drafted by row 4c's worker in this file; the supervisor read them against the commit and concurs without amendment, including the classification *different in kind*: trips 1-5 were guards that did not look, trip 6 is a guard that looked and a fact that then moved. Three additions for the register. **(1) Q56 is founder-visible and is recorded as such** in the row's rulings file; the supervisor's default while it is open is the worker's *cheap half* -- `resolve_type` reports the redirect with a warning, rather than silently at 1.0, when the two extents no longer agree (Rule U applied at the read; a new warning value under R3) -- taken in a row of its own together with Q57 and Q58, under a **staleness** axis added to `check_merge_guard.py` first, so the change lands under a gate that can see it. The *expensive half* -- refusing to answer, which changes what the registry declines to serve under `INTERFACE.md` s5.3 -- stays the founder's. **(2) The checker's record is now two-for-two against read-path trips**: it exited 0 through trips five and six because its fixtures cannot pose paging or staleness; that is not an argument against the checker, which found trip four and corrected a wrong human verdict on `reinstate` in trip six, but it is the argument that constraint 7's loop and constraint 8's gate are both load-bearing and neither substitutes for the other. **(3) Suite:** 226 ids, sync `549 passed`, async `584 passed`, three legs, five checkers green -- above the row's floor of 220 / 537. **Recommendation to the founder: continue, and the `stop` option is put to him explicitly this time rather than left standing** -- six trips in test, none in a real merge, every one narrower than the last (round 3's were about whether a ruling was applied consistently, not whether the code works), and the one that closes the class is Q56, which is his to rule.


## The kill-row trip — SEVENTH instance (2026-08-30, row 4d's first adversarial round), and it is a third kind

**What happened [Observed, row 4d].** Reproduced end to end against the shipped `Registry` on **both fully-capable legs** (sqlite, Postgres) and on the **async mirror**, with the full suite green and all five gates exiting 0 — `check_merge_guard.py` included, on the very axes row 4d had just added for the sixth trip.

1. `commentable` `{aaa_note}` and `searchable` `{aaa_note, bbb_memo}` — two live predicates whose extents genuinely differ;
2. `merge_types` refuses that pair **non-overridably**, under all five acknowledgements;
3. `commentable` is retired — an ordinary, permitted governance act;
4. `import_types` writing `aliases: ["commentable"]` is **refused** `import_refused:predicate_merge` — row 4c's fourth-trip guard, working;
5. `import_types` writing `aliases: ["Commentable"]` is **written, with no refusal and no warning**;
6. `resolve_type("commentable")` answers **`searchable` at confidence 1.0** — which `INTERFACE.md` §5.3 calls a **guarantee** — with the two extents still different, and **no `identity_stale`**, because row 4d's own staleness gate is the same comparison.

**Why this trip is a third kind.** Trips 1–5 were one sentence: *the guard did not look properly* — at an unknowable extent, at an empty one, at all, through a different field, at a partial page. Trip 6 was: *the guard looked correctly, and then the fact changed.* **Trip 7 is: the guard and the resolver disagree about what THE SAME WORD is.**

Every alias guard finds its collision by an exact byte comparison — `rec.name == alias`, `alias in rec.aliases`, `candidate in entry.aliases`. The shipped `DeterministicResolver` scores `_norm(candidate)` against `_norm(alias)`, lowercasing and collapsing every run of non-`[a-z0-9]` to `_`. So `'Commentable'` is a word the guards have never heard of and the resolver rates **1.0**.

> **Every operand of Rule U so far has been about the EXTENT comparison. Nothing in this project had ever written down the identity of the NAME.** Unknowable is not equal, empty is not equal, partial is not equal, stale is not equal — and *one word is not one string*.

**The non-canonical spelling is the real one.** `import_types` is UC1 Tenshen's own Foundry migration path (`PACKAGE.md` §7) and UC3's Socrata shape, and a real export's field labels arrive as `"Status"` and `"Processing Date"`, not as `snake_case`. Reproduced in UC3's shape as well: `merge_types` refusing `predicate_merge` non-overridably, `import_types(aliases=["Status"])` accepted, `resolve_type("status")` answering at 1.0.

**Fixed by giving the registry ONE notion of *the same word*.** `identity_key` is published beside the resolver — not copied into `registry.py` — and `_alias_identity_breach`, `_alias_clash`, `_alias_holder` and `resolve_type`'s staleness gate are all keyed to it. The arrangement is the fix: the registry must not be *able* to disagree with the scorer that delivers its 1.0. Pinned by **`C10-15`**, and `check_merge_guard.py` gains a **spelling** axis that imports the resolver's own `_norm` rather than restating it.

**Six more defects of the same family, found beside it in the same round and fixed in the same commit.** The round ran two lenses: one attacking row 4c's five guard changes (which `4C-RUN.md` §6.6 predicted *"would find something"*), one attacking row 4d's own. Between them **7 BLOCKING, 5 MAJOR, 2 MINOR**, every one reproduced before it was believed.

| what | where | why it is the same family |
|---|---|---|
| **a successor that does not exist YET** | `retire(successor=)` | every one of §5.10's identity guards is evaluated against the successor's **row**, so naming a word before it is registered gave all three nothing to compare — and the word was then created by an ordinary proposal, after which `resolve_type` cashed the redirect at 1.0. **A predicate retired toward a word that arrived as an `entity` answered a question about one kind with an entry of another at 1.0**, which is refusal #3 walked past entirely, and Q56's default cannot warn about it. New refusal **`successor_unregistered`**, non-overridable, `force` included. `C9-22` |
| **`C16-06`'s invariant at four unasked doors** | `import_types` (the row's own **name**), `approve` (`_write_approved` re-checked nothing — and **R40 forces every predicate down that two-step path**), `reinstate` (never asked §5.9b's collision question its sibling asks on the same field), and a **truncated** collision scan | *two live entries holding one word* is mechanism 4 itself, and row 4c closed exactly one of the five doors to it. `C4-12`, `C9-23`, `C12-12` |
| **Rule U's third operand missing from the sixth trip's own guard** | `_alias_holder` / `_alias_clash` discarded `_active_page`'s `why` | a truncated look read as *"the word is free"*. **Reported, not refused**: refusing bans `propose_type` on every paging backend, which `C3-13` caught within one suite run — `C10-09`'s lesson one call along. New warning `alias_check_incomplete`. `C4-12` |
| **the wrong story for the right outcome** | `merge_types`' transferred-alias check sat *below* the overridable guards | a caller asking without acknowledgements was told `no_consumer_evidence`, `overridable=True`, about a collapse that never moves. `C9-19`'s defect class exactly, and `retire` had it fixed by D-4c-5 **in the same commit that added this check** |
| **refusal #1 not at the alias door** | `_alias_identity_breach` carried §5.10's #2 and #3 only | `C9-20` ruled #1 an **identity** guard; the alias write produces the identical 1.0 redirect, so the pair `merge_types` and `retire(successor=)` both refuse non-overridably collapsed through the third door |
| **one hop instead of the chain** | `_declared_predicate_moved` (R55) | a vocabulary merged **twice** — the ordinary outcome after two curation passes — left the first word unwarned |

**Supervisor judgment.** The design is *still* not what tripped: `namespace` is untouched across all seven and `cross_namespace_merge` still refuses on live NYC data. And the honest counterweight is now three deep: **`check_merge_guard.py` exited 0 through trips five, six and seven**, each time because its fixtures could not pose the question — paging, then staleness, then *the alphabet*. That is not an argument against the checker, which found trip four and has now been extended three times by what the loop found; it is the argument that constraint 7's loop and constraint 8's gate are both load-bearing and neither substitutes for the other, **restated for the third consecutive row**.

What is new, and what the founder should weigh: **the seventh trip was not in code row 4d wrote.** `_alias_identity_breach`'s byte comparison shipped in row 4c and `_alias_holder`'s in row 4c's third round; row 4d's contribution was to inherit the same key in a new gate. Five of the twelve findings are in row 4c's guards, which is the first time this loop has found a defect **outside** what the current row touched — `4C-RUN.md` §6.6 recorded that across three rounds *"not one finding was in code this row did not touch"*, and that has now stopped being true. Read one way that is reassuring (the loop reaches further than the diff); read another it says the guards laid down under time pressure in row 4c need the review they did not get.

**Recommendation to the founder: continue, and the `stop` option is put explicitly again.** Seven trips in test, none in a real merge. The one that closes the *class* is still **Q56**, which is his to rule; this row shipped its cheap half.

**Supervisor countersignature (2026-08-30 05:12, commits `0f1d647`, `87aac1e`, `843af71`).** The seventh-trip record and the judgment above were drafted by row 4d's worker in this file; the supervisor read them against the three commits and concurs without amendment, including the classification as a *third kind* (the guard and the resolver disagreeing about what the same word is) and the shape of the fix -- one `identity_key` published beside the resolver, so the registry is not *able* to disagree with the scorer that delivers its 1.0, rather than a normalisation copied into a sixth place. Three additions for the register. **(1) Rule U's operands now span two comparisons**: four about the extent (unknowable, empty, partial, stale) and one about the name (one word is not one string); the spelling axis in `check_merge_guard.py` imports the resolver's `_norm` rather than restating it, which is the same arrangement as the fix and the only one that cannot drift. **(2) Five of the twelve findings were in row 4c's guards, not this row's diff** -- the first time the loop has found defects outside what the current row touched. The supervisor rules from that: row 6b's guard extraction (R53) is **the review those five guards did not get** -- its first adversarial lens is the extracted function against all seven trip records, before any ACTIONS build id is written -- and the row that extracts them may not also change what they compare. **(3) Suite:** 236 ids, sync `569 passed`, async `604 passed`, three legs, five gates with five axes -- above the row's floor of 226 / 549; and the reference Postgres leg's fixture now drops its schema at teardown, after 18,694 leaked `oo_*` schemas were found to be the cause of three backend segfaults during this round (recorded in `4D-RUN.md`). **Recommendation to the founder: continue; the `stop` option is put explicitly for the third time.** Seven trips in test, none in a real merge; the class-closing question is still Q56, and this row shipped its cheap half.


## The kill-row trip — EIGHTH instance (2026-08-30, row 4d's third adversarial round)

**What happened [Observed, row 4d].** Four ordinary calls, no alias, no import, no merge. Reproduced on both fully-capable legs and the async mirror, with the full suite green and **all five checker axes exiting 0**.

1. `commentable_` goes live as a `kind="predicate"` entry and a type declares it — extent `{note}`;
2. `commentable_` is **retired** — *an ordinary, permitted governance act*, the fourth trip's own words;
3. `commentable` is proposed and approved, and a type declares it — extent `{doc}`. **Accepted: no refusal, no warning**;
4. `resolve_type("commentable_")` → **`commentable` at confidence 1.0, warnings empty** — on a pair `merge_types` refuses **non-overridably** under all five acknowledgements.

`identity_key("commentable_") == identity_key("commentable")`. By the registry's own published notion of *the same word* — the one the **seventh** trip minted, one round earlier — these are one word.

**Why it is the seventh trip alive at HEAD rather than a new class.** §5.4's rule *"a retired name is not reusable; silently reusing a retired word is mechanism 4 with a time delay"* was a **byte** comparison (`get_type(namespace, name)`), and round 2's keyed guard `_alias_holder` scans **active** rows only. So the retired half of the **name** door was never keyed at all. The reason string says both things in one breath: *"'commentable' matches at 1.0; 'commentable_' … the name is not reusable"*.

**And the registry already knew.** `retire("commentable_", successor="commentable")` on the same store is refused *"a word cannot be its own successor"*. **One function in this file said the two words are one word; the door that let both exist compared bytes.**

> `NAME_RE` is `^[a-z][a-z0-9_]{0,63}$`, so `commentable_`, `commentable__`, `bike__lane` and `borough_` are all **legal names** and all variants by `identity_key`. UC3 is dozens of agencies normalising their own column headers into one namespace: `"Borough"` → `borough` and `"Borough:"` → `borough_` is the ordinary case, not a contrivance.

**Why the checker exited 0 — and this is the finding about the finding, for the fourth consecutive time.** `_SPELLINGS` held `("Commentable", "COMMENTABLE", "commentable ", "commentable-")`, and **not one of them is `NAME_RE`-legal**. Every one can therefore only ever arrive in the `aliases` field, so the spelling axis could pose *"can a variant reach the alias door?"* and never *"can a variant be a row's own NAME?"* **`name` is an identity field too** — Part A's `IDENTITY_FIELDS` names `successor` and `aliases` and scans `status` narrowly, and two rows whose names share one key are two entries answering one word, which is `C16-06` and mechanism 4. The axis now carries `commentable_` and a **name-write** probe beside the alias-write one.

**Fixed** by giving the name door the same key, over rows of **every status**: `_word_rows` is a paged, keyed scan that includes retired rows, and `propose_type` and `import_types` both hand back the tombstone. Pinned by **`C10-19`**, with its narrowing — `commentary` is not `commentable`, and a genuinely different word is still free.

**A second defect of the same family, and it failed in BOTH directions.** `identity_key` collapses every run of non-`[a-z0-9]`, so **every word with no ASCII alphanumerics maps to the empty string**, and `difflib` rates two empty strings **1.0**. An alias `状态` ("status") made `resolve_type("类型")` ("type") — a *different* word — answer that entry at 1.0; and the second agency's legitimate `类型` was then **refused** `alias_collision`. **The identity function was manufacturing mechanism 4 instead of preventing it**, on UC3's multi-agency catalogue and UC2's punctuation-only CMS headers. Fixed by `same_word`: an empty key is not a word and is never equal to anything, which is Rule U on the identity of the **name**. Pinned by **`C4-14`**.

**Four more, all found in the same round and fixed in the same commit.** `resolve_type` omitted `kind=` from its successor lookups and **raised `AmbiguousKind`** out of the call designed against mechanism 2, on a store `PACKAGE.md` §4.1 explicitly blesses — `retire`'s own guard says why that is wrong and the lesson had not travelled. Past `_IDENTITY_CHAIN_CAP` the same call answered *"nothing ACTIVE in the vocabulary fits this"* while blaming **namespaces**, where `_identity_closure` and `list_types` both name the cap (`C3-16`). `retire(successor=)` accepted a successor that was **itself retired**, leaving the old word resolving to nothing — §5.10's promise destroyed with no refusal and no warning, on an act `merge_types` has always refused (`C9-25`). And `successor_unregistered` was returned for the self-successor case, a sentence that says *register the successor first* about a word that is registered, live, and is the type itself (`successor_is_self`).

**Supervisor judgment.** Eight trips. The design is *still* not what tripped — `namespace` is untouched across all eight and `cross_namespace_merge` still refuses on live NYC data — and every trip was caught in test, none in a real merge. Two things are new and both belong to the founder's `stop` question:

1. **The checker has now exited 0 through trips five, six, seven and eight.** Each time for the same reason in a different dress: its fixtures could not pose the question — paging, then staleness, then the alphabet in the `aliases` field, then the alphabet in the `name` field. That is not an argument against the checker, which found trip four and has been extended by every trip since; it is the fourth consecutive restatement that **constraint 7's loop and constraint 8's gate are both load-bearing and neither substitutes for the other**.
2. **Trips seven and eight are one defect found twice, one round apart.** Seven was the guards and the resolver disagreeing about what a word is; eight is the *same* disagreement in the one door the seventh trip's fix did not reach. A fix that publishes a shared key is only as good as its application, and the honest reading is that row 4d shipped the key and missed a caller — which is the sixth trip's own shape (*a guard written for one call over a fact more than one call can change*) applied to a fix rather than to a guard.

**Recommendation to the founder: continue, and the `stop` option is put explicitly for the fourth time.** Eight trips in test, none in a real merge; every one narrower than the last; and the question that closes the *class* is still **Q56**, which is his. This row shipped its cheap half.

**Supervisor countersignature (2026-08-30 08:20, commits `f06144c`, `fd919de`, `3f0c2f3`).** The eighth-trip record and the judgment above were drafted by row 4d's worker in this file; the supervisor read them against the three commits and concurs without amendment -- including the classification *the seventh trip alive at HEAD, not a new class*, and the reading of the checker's record: exited 0 through trips five, six, seven and eight, each time because its fixtures could not pose the question. Three additions for the register. **(1) `name` is an identity field**, alongside `successor`, `aliases` and `status`; the checker's spelling axis now drives a `NAME_RE`-legal variant into the name door as well as the alias door, and the same-word rule is applied over rows of every status (`_word_rows`, `C10-19`). **(2) `same_word`'s rule that an empty key is never equal to anything (`C4-14`) is Rule U on the identity of the name; the question it leaves open -- whether the registry should hold a word it cannot compare at all -- is Q68, carried to the founder with the shipped behaviour as the default.** **(3) Correction to the seventh-trip countersignature above:** its suite figures (236 / 569 / 604) were a mid-round-1 snapshot; `843af71` itself was 238 ids / sync 573 / async 608 (worker's note at landing). The row ended at **249 ids, sync `596 passed`, async `631 passed`**, three legs, five gates, six checker axes -- the floor from here. Row 4d landed at `3f0c2f3`; rulings on its questions are in [`2026-08-30-4d-rulings-R61-R66.md`](2026-08-30-4d-rulings-R61-R66.md). **Recommendation to the founder: continue with row 6b -- which is ruled to be the review these guards did not get -- and the `stop` option is put explicitly for the fourth time.** Eight trips in test, none in a real merge; the class-closing question is still Q56, now with three options and the permanence evidence (R65) on the record.


## The kill-row trip — NINTH instance (2026-08-30, row 6b's first adversarial round)

**What happened [Observed, row 6b].** Five ordinary calls, no merge, no retirement toward a successor, and **a pair whose extents agree**. Reproduced on both fully-capable legs (sqlite, Postgres) and on the **async mirror**, with the full suite green and `check_merge_guard.py` exiting 0 on all three legs — including the six axes and the Part A2 caller-reachability check row 6b had just added.

1. `commentable` and `gamma` are `kind="predicate"` entries whose written extents are `{aaa_note, bbb_memo}` — **non-empty and identical**, so §5.10's refusal **#2 passes honestly**. This is not another #2 evasion;
2. `commentable` declares `meta_p`, and a consumer gates on `meta_p` — so `commentable.gates_on == ['svc:meta']` and `gamma`'s is empty. **The gate is not the aliased word**, so an alias cannot equalise the two sets;
3. `commentable` is **retired** — *an ordinary, permitted governance act*, the fourth trip's own words;
4. `import_types` writes `{"name": "gamma", "kind": "predicate", "aliases": ["commentable"], "status": "active"}` — **creating `gamma` in the same call that writes the alias**. Written, with **no refusal and no warning about the collapse**;
5. `resolve_type("commentable")` → **`gamma` at confidence 1.0**, which `INTERFACE.md` §5.3 calls a **guarantee** — on a pair `merge_types` refuses `different_consumer_sets` **non-overridably** under all five acknowledgements, and that `retire(successor=)` refuses too.

**Why refusal #1 did not fire, and it is a fourth kind.** Trips 1–5 were *the guard did not look properly*. Trip 6 was *the guard looked correctly and then the fact changed*. Trips 7 and 8 were *the guard and the resolver disagree about what the same word is*. **This one is: the guard had nothing on the right-hand side, and compared the left against ITSELF.** `_alias_identity_breach` read the row being aliased onto to get its consumer set — and `import_types` routinely creates that row in the same call, so `self.adapter.get_type(...) or other` fell back to `other`, the two sets were equal **by construction**, and #1 could never fire on a new row. **[Observed]** the two controls that name the guard which does not stop it: with the row already existing the same import is refused `import_refused:different_consumer_sets`; with the old word still live it is refused `alias_collision`. The gap is exactly one door.

> **Rule U's operands now span three comparisons.** Four about the EXTENT — unknowable (trip 1), empty (trip 2), partial (trip 5), stale (trip 6). Two about the NAME — *one word is not one string* (trip 7), *an empty key is not a word* (trip 8). And now one about the CONSUMER SET: **a row that does not exist has none to READ, and that is not the same as having none.**

**Why the checker exited 0 — the finding about the finding, for the fifth consecutive time.** Part B drives every collapsing caller through every extent state, and `import_types`' row builds its fixture by retiring `commentable` and then importing an alias onto **`searchable`, which the fixture has already seeded**. So the probe could pose *"can an alias collapse two EXISTING identities?"* and never *"can it collapse an existing one into a row this very call is creating?"* Row 6b's own Part A2 — *does a caller judged to collapse actually reach the shared guard?* — answered **yes** for `import_types`, correctly and irrelevantly: it reaches the guard, and the guard had nothing to compare. **A checker only asks the questions its fixtures can pose**, and this is the fifth dress that sentence has worn.

**Where it came from is worth recording.** Ruling **R53** put the guard extraction in this row and the seventh-trip countersignature ruled what that extraction is: *"the review 4c's guards did not get — its first adversarial lens is the extracted function against all the trip records."* The extraction did not introduce this defect — a reviewer verified it live at `946658c`, the commit before — but putting five copies of §5.10's refusals side by side is what made the fallback **visible**, and row 6b raised it as **Q69** in the extraction's own docstring before any reviewer read the row. **The first adversarial lens then walked it.** That is the ruling working exactly as written.

**Fixed with a COMPUTED set, not a refusal, and the distinction is this project's most-repeated lesson.** *"A guard with nothing to compare has not said the collapse is safe"* is `successor_unregistered`'s rule (trip 7) and was the obvious transposition — and it is **wrong here**, because there IS something to compare: **the incoming row declares its own `predicates`**, so the consumer set it will have the moment it is written is a *fact*, not an unknown. `Registry._gates_on` computes it, from the same rule `_consumer_report` uses and stated once. Refusing instead would have banned `import_types` from writing **any** aliased new row on **any** backend — which is `C10-09`'s lesson, `C3-13`'s and `C12-13`'s, learned three times in row 4d alone: *never turn "we could not finish looking" into a refusal* — and worse, it would have turned **"we did not look"** into one. Pinned by **`C12-14`**, with **`C12-15`** for the half a careless fix deletes: an import creating a row whose computed consumer set **matches** the word it aliases is still written.

**And the fix was itself defective, twice, inside one suite run.** It compared a computed set against one **read off a stored record** — and on `indexes_membership=False` a record's `predicates` come back empty, so it compared a fact against an absence and refused a legal import. That is the **first** trip's operand pointing the other way: *unknowable is not equal, and it is not DIFFERENT either*. Both sides are computed the same way now, and where the backend cannot report membership at all refusal #1 is **skipped** rather than refused, with the residual stated (**Q69**). `check_capability_matrix.py` found that; `C12-15`'s narrowing found the second cut, which passed `None` where the guard needed skipping. **Row 4d's round 2 found five defects inside round 1's fixes; this is the same pattern arriving on schedule, and both were caught by gates rather than by reading.**

**Supervisor judgment and countersignature (2026-08-30 13:30, commits `07ac4e7` and `ef87025`).** The record above was drafted by row 6b's worker; the supervisor read it against both commits and concurs, and classifies the ninth trip as the worker does -- a fourth kind, *nothing on the right-hand side*. Four things for the register. **(1) The design is still not what tripped.** `namespace` is untouched across all nine and `cross_namespace_merge` still refuses on live NYC data; every trip was caught in test, none in a real merge. **(2) This trip is R53 working, not R53 failing.** It predates 6b (live at `946658c`), the extraction made the fallback visible, the row raised it as Q69 before any reviewer read it, and the review the extraction row was ruled to be walked it. That is the sequence the seventh-trip countersignature asked for, and it is the first trip found by a review that was *scheduled* for the class rather than by a reviewer who happened to reach it. **(3) The fix is the right shape and the register should say why in one sentence:** *a guard that cannot read a fact must compute it if the fact is computable, skip and say so if it is not, and never refuse for want of looking* -- the ninth trip's fix computed (`_gates_on`), the seventh's refused (`successor_unregistered`) because a successor's future extent is not computable, and the difference is whether the missing fact is a declaration in the caller's hand. **(4) The checker's record is now five-for-five against read-path trips** (paging, staleness, alphabet in `aliases`, alphabet in `name`, a row that does not exist yet); Part A2 answered its question correctly and irrelevantly, which is the honest limit of a reachability check -- it cannot see what a reached guard had to compare. The lesson stands for the fifth time and is not restated further: constraint 7's loop and constraint 8's gate are both load-bearing. **Recommendation to the founder: continue, and the `stop` option is put explicitly for the fifth time.** Nine trips in test, none in a real merge; the one that closes the *class* remains Q56, the founder's, with three options and the permanence evidence on the record.


## The kill-row trip — TENTH instance (2026-08-30, row 6b's second adversarial round)

**What happened [Observed, row 6b].** Five ordinary calls, no merge, and **a pair on which BOTH identity guards pass honestly at the moment they are asked**. Reproduced on sqlite, on Postgres and on both async legs, with the full suite green (`418 passed`) and `check_merge_guard.py`, `check_spec_drift.py`, `check_links.py` and `check_capability_matrix.py` all exiting 0.

1. `commentable` and `gamma` are `kind="predicate"` entries whose written extents are `('aaa_note', 'bbb_memo')` — **non-empty and identical**, so §5.10's refusal **#2 passes honestly**;
2. `gamma` **already exists**, declaring `meta_p`, and a consumer gates on `meta_p` — so `commentable.gates_on == gamma.gates_on == ['svc:meta']` **at guard time**, and refusal **#1 passes honestly too**;
3. `commentable` is **retired** — *an ordinary, permitted governance act*;
4. `import_types` writes `gamma` with `predicates: []` **and** `aliases: ["commentable"]`. **Written, with no refusal and no warning**;
5. post-import `gamma.predicates` is `()`, so the two sets are now `['svc:meta']` against `[]` — and `resolve_type("commentable")` answers **`gamma` at confidence 1.0**, on a pair `merge_types` **and** `retire(successor=)` both refuse `different_consumer_sets` **non-overridably**.

**Why it is not a repeat of the ninth, and why it is worse.** The ninth trip was refusal #1 having *nothing* on its right-hand side and comparing the left against itself. Row 6b's round 1 closed that by **computing** the incoming row's consumer set from its declared `predicates` — and **applied the computation on one branch only**. Where the aliased-onto row already exists the guard took the other branch and read `_consumer_report` off the **stored** row — *the row this same call is about to overwrite*, because `import_types` writes `predicates` from the incoming dict. **The guard evaluated a state the call destroys.**

> **The same declared row, the same alias, the same final state — and the guard answered two ways depending on whether the name happened to exist already.** That is not a missing operand; it is one door disagreeing with itself.

**It is the SIXTH trip's diagnosis applied to a FIX rather than to a guard** — *a guard written for one call, over a fact that more than one call can change* — which is also the **eighth** trip's shape, where `identity_key` was published and reached five callers out of six. Row 4d's own countersignature named that pattern: *"a fix that publishes a shared key is only as good as its application."* Round 1 computed a shared set and applied it to one of two branches.

**Why the checker exited 0 — the finding about the finding, for the SIXTH consecutive time.** Part B's `import_types` fixture retires `commentable` and imports an alias onto **`searchable`, which the fixture has already seeded** — so it poses *"can an alias collapse two existing identities?"*, `C12-14` poses *"can it collapse one into a row this call is creating?"*, and **neither poses *"can it collapse one into a row this call is REWRITING?"*** Row 6b's own new Part A2 — *does a caller judged to collapse actually reach the shared guard?* — answered **yes** for `import_types`, correctly and irrelevantly, for the second round running.

**Fixed** by making refusal #1 compare the consumer sets **this write will produce**, on both branches and computed the same way on both sides, from what the write leaves behind rather than from what it is about to erase. Pinned by **`C12-16`**; `C12-14` and `C12-15` are unchanged and still pass.

**Two more BLOCKING of the same family were found beside it in the same round, and both are one fix applied at one call site out of two.** `_compensated_by` was made to return `(id, why)` by round 1 and wired into `invocations()`; **`review_invocation` destructured the sentence into `_why` and dropped it**, so a compensated invocation still read back `outcome="applied"` past the bound — the round-1 defect verbatim, surviving in the call site the fix did not visit. And the action `payload_schema` was looked up by the **family's name** instead of by the **schema name the family declared** (the edge side does the latter), so the enforce-mode schema a family named was never consulted, a schema keyed by family name was enforced although the family never named it, and `payload_schema_unregistered` — minted in this row to *end* R34's inert field — fired as a **confident false negative** about a schema that was registered. Pinned by **`C19-62`** and **`C19-63`**.

**And the walk itself is gone rather than re-bounded.** `_compensated_by` read the ledger to derive §9's backward pointer, which produced three defects across two rounds — a bound that reported the wrong `outcome`, a sentence dropped at the second call site, and **200,020 row reads for twenty returned rows**. Primitive 21 gains a `compensates` filter and the derivation is one indexed lookup: no bound to lie about, no sentence to drop, no walk. That is round 2 of the *specification* row's own finding one derivation along — *the reads with no push-down were exactly the governance reads.*

**Supervisor judgment and countersignature (2026-08-30, commit `6fd065d`; countersigned after the ontoloche rename).** The record above was drafted by row 6b's worker; the supervisor read it against the commit and concurs, including the classification: not a missing operand but **one door disagreeing with itself** -- the ninth trip's fix computed a shared answer and applied it to one branch of two, which is the sixth trip's diagnosis (*a guard written for one call, over a fact more than one call can change*) landing on a FIX, exactly as the eighth trip's did. Three additions for the register. **(1) The project has now counted where its next defect lives, and the count is the policy:** 3e (4 of 10 BLOCKING inside a prior fix), #4 round 3 (2 of 4), 4d round 2 (5 inside round 1's fixes), 6b round 2 (the tenth trip inside the ninth's fix, plus two more one-call-site-out-of-two defects). The fix-auditor lens is therefore no longer a lens choice but a standing requirement: **every round after a fix round begins with a lens pointed at that fix**, and this sentence is the ruling. **(2) The right fix shape is again the register's rule one step further:** compare the state a write will PRODUCE, computed the same way on both sides -- not the state it is about to destroy. That subsumes the ninth trip's compute-or-skip rule and belongs beside it. **(3) The checker's fixtures could not pose the question for the sixth consecutive trip** (existing-row rewrite, after create-in-same-call, name-door spelling, alias-door spelling, staleness, paging); Part A2 was right and irrelevant for the second round running. The standing conclusion is unchanged and this register stops restating it. **The design is still not what tripped** -- `namespace` untouched across all ten, `cross_namespace_merge` still refuses on live NYC data, every trip caught in test, none in a real merge. **Recommendation to the founder: continue, and the `stop` option is put explicitly for the sixth time.** Ten trips; the class-closing question remains Q56 (three options and the permanence evidence on the record), and the write-side rule that would close THIS family -- guards evaluate the post-state of the write they guard -- is Q-numbered by the worker in 6B-RUN for the landing rulings.


## The kill-row trip — ELEVENTH instance (2026-08-30, row 6b's third adversarial round)

**What happened [Observed, row 6b].** Seven ordinary calls, no merge, and **a pair on which both identity guards pass honestly at every moment they are asked**. Reproduced on sqlite, on Postgres and on both async legs, with the full suite green (sync `746 passed`, async `785 passed`, three legs) and `check_merge_guard.py` exiting 0.

1. `commentable` and `gamma` are `kind="predicate"` entries with extents `('aaa_note', 'bbb_memo')` — **non-empty and identical**, so §5.10's refusal **#2 passes honestly throughout**;
2. `commentable` is **retired** — *an ordinary, permitted governance act*;
3. `import_types` writes `gamma` with `predicates: ["meta_p"]` and `aliases: ["commentable"]`. **Correctly allowed**: no consumer exists yet, both gate sets are empty, and the alias is legal;
4. `gamma` is **retired** — the alias goes dormant. Also ordinary;
5. `register_consumer(svc:meta, gate="meta_p")`. **The world moves**: `gamma` now gates `['svc:meta']` and `commentable` gates `[]`;
6. `reinstate("gamma")` → **`TypeEntry`, no refusal, no warning**, `aliases=('commentable',)`;
7. `resolve_type("commentable")` → **`gamma` at confidence 1.0** — up from `proposal / None / 0.4706` the call before — on a pair `merge_types` refuses `different_consumer_sets` **non-overridably**, and `retire(successor=)` refuses too.

**Why it is the ninth and tenth trips' own defect, at the doors they did not come through.** Refusal #1's operand — the target row's declared `predicates`, without which `_gates_on`'s `member_of` is empty and **every consumer gating on a predicate the target itself declares is invisible** — was passed at **one of `_alias_identity_breach`'s four call sites**:

| caller | `declared_predicates` |
|---|---|
| `retire(successor=)`, transferred aliases | **not passed** |
| `reinstate`, dormant aliases | **not passed** |
| `merge_types`, transferred aliases | **not passed** |
| `import_types` | passed — the ninth and tenth trips' fix |

The async mirror was identical. So the sixth trip's diagnosis — *a guard written for one call, over a fact more than one call can change* — has now been applied to a **fix** for the **third consecutive round**: trip 9 was the missing operand, trip 10 was the operand supplied on one branch of two, and trip 11 is the operand supplied at one call site of four. **That is new information for the founder's `stop` question and it is stated as such rather than folded into the count.**

**Why the checker exited 0 — and this reason is sharper than the previous six.** It is countable rather than descriptive: `check_merge_guard.py` contained **zero** occurrences of `register_consumer` and zero of `Consumer(`. **No fixture, on any leg, at any door, had ever registered a consumer** — so both gate sets were empty in every probe and `different_consumer_sets` passed **vacuously everywhere**. Trips five through ten were *"its fixtures could not pose the question"*; this one is stronger: **refusal #1 was a guard this checker had no way to fail on at all**, in the file built to enumerate exactly these guards. `C9-20` is the ruling that makes #1 an identity guard rather than an evidence one — and identity guards are what Part B exists to drive.

**Fixed** by making `declared_predicates` a **required keyword** on `_alias_identity_breach` — the treatment ruling **R64** gave `_extent`'s `identity`, and for the same reason: *no caller can take a reading by accident* — and by passing the target row's own predicates at all four sites. Pinned by **`C12-17`**. `check_merge_guard.py` gains a **seventh axis, CONSUMER SETS**, driving refusal #1 through all four collapsing callers on every leg; **proved by mutation** — reverting the fix at `reinstate` alone turns that row `FAILED` and the checker exits 1.

**A second BLOCKING of the same family, at the layer this row added.** `ACTIONS.md` §2.3's predicate exclusion — *"the exclusion is general or it is nothing"* — rested on `ref_kind`, which returns the kind **the caller wrote on the reference** and never checks it against the stored row. A `TypeRef("default", "entity", "commentable")` naming a real capability predicate, or a misspelled `"Predicate"`, walked straight past it: **[Observed]** `merge_capabilities(commentable, searchable)` reaching `verdict="allowed"` at `preflight` and **recorded `applied`** on two live predicates. That is the **seventh** trip's diagnosis one surface along — *a guard comparing a byte where the registry holds a stored fact* — and row 6b had hardened `ref_shape` against an unrecognised **shape** while leaving the **kind** to the caller's word. Fixed by refusing a kind the registry can see is false; pinned by **`C19-71`**.

**Supervisor judgment and countersignature (2026-08-30, commit `d985621`).** The record above was drafted by row 6b's worker; the supervisor read it against the commit and concurs. Trips nine, ten and eleven are one defect found three rounds apart -- the operand absent, the operand on one branch of two, the operand at one call site of four -- and the register now names the rule the three fixes converged on, because it is R64's treatment generalised: **an operand a guard cannot safely default is a REQUIRED KEYWORD, so a missing reading fails at the call site instead of passing vacuously.** Applied to `_extent(identity=)` by R64, to `_alias_identity_breach(declared_predicates=)` here; the ACTIONS build row and any future guard inherit it as a standing rule of this register. Two more additions. **(1) The checker's reason this time is countable, not descriptive, and it is the sharper indictment:** zero occurrences of `register_consumer` in the file meant refusal #1 could not be failed by any fixture on any leg -- not a question unposed but a guard unfalsifiable; axis seven (CONSUMER SETS) closes it and is proved by mutation, which is the standard every axis should meet from here. **(2) The ACTIONS-layer sibling** (`ref_kind` trusting the byte the caller wrote over the row the store holds) is the seventh trip's diagnosis one surface along, and it walked a capability-predicate merge to `applied` through the layer built to govern actions -- the kill row's noun reached through the newest door in the codebase. **New information for the founder's stop question, stated as the worker stated it:** the sixth trip's diagnosis has landed on a FIX in three consecutive rounds. The honest reading cuts both ways -- the loop caught each within one round of its introduction, and each fix narrowed (branch, then call sites, then the keyword that ends the class of missing-operand fixes); but three rounds of incomplete fixes on one guard family is the closest this register has come to evidence for `stop`, and it is put to the founder as such rather than smoothed. **Recommendation: continue through 6b's landing** -- the required-keyword rule plus axis seven close the missing-operand family mechanically, which no earlier fix could claim -- **and the stop option is put explicitly for the seventh time.** Eleven trips in test, none in a real merge; the class-closing question for the read side remains Q56, the founder's.


## The kill-row trip — TWELFTH instance (2026-09-01, row 6c's first adversarial round), and the first one a lens proved by BISECT

**What happened [Observed, row 6c].** Three ordinary calls, no merge, and **every identity guard passing honestly at the moment it is asked**. Reproduced on sqlite, on Postgres and on the async mirror, with the full suite green and `check_merge_guard.py` exiting 0 on all three legs — *including the eighth axis row 6c had just added for this very door.*

1. `alpha`, `beta` and `gamma` are `kind="predicate"` entries whose written extents are `{aaa_note, bbb_memo}` — **non-empty and identical**, so §5.10's refusal **#2** passes honestly throughout;
2. `import_types` writes `aliases: ["zeta"]` onto `alpha`. **Correctly allowed**: the extents agree, no consumer exists, and the word is free;
3. `retire("alpha", successor="beta")`. **Correctly allowed**, and ruling **R75**'s new write transfers `zeta` onto `beta` — which is the whole point of R75;
4. `retire("alpha", successor="gamma")` — **`alpha` is ALREADY retired**. Returned a `TypeEntry`, no refusal, no warning, and transferred `zeta` onto `gamma` **as well**;
5. `beta.aliases == gamma.aliases == ('zeta',)` — **two ACTIVE rows answering to one word**, which is `C16-06`'s whole-store invariant and mechanism **4** itself. `merge_types(beta, gamma)` refuses that pair `predicate_merge` **non-overridably**, and `resolve_type("zeta")` answers one of them at **confidence 1.0** — *which* one decided by nothing but page order, because `_alias_map` does `holders.setdefault(...)` and the first row in the scan wins.

**And the two answers disagree with each other in the same store.** `resolve_type("alpha")` → `gamma` **with `identity_stale`**; `resolve_type("zeta")` — the word `alpha` answered to, moved by the very same call — → `beta` **with no warning at all**, because a transferred word has no row of its own and `_identity_stale` short-circuits on its first line. **Q56's cheap half is structurally blind to exactly the words this row started writing.**

**The missing guard is `rec.status`, and this call never had it.** `retire` reads `.status` exactly twice and **both times on the successor**. It never asked whether the row it is retiring is already retired. Before R75 a repeat retirement merely rewrote the tombstone; R75 attached an alias write to it.

> **Why it is a new sentence rather than the fourth repeat.** Trip 9 was *the operand absent*; trip 10 *the operand on one branch of two*; trip 11 *the operand at one call site of four*. **This is the guard evaluated ONCE for a call that can run TWICE.** The register's own rule from trip ten — *the write a guard permits and the write a call performs must be the same write* — holds **per call** and fails **across calls**, because `rec.aliases` is **not consumed** by the write it authorises: the tombstone keeps its words by design (`INTERFACE.md` §5.8), so nothing in the state stops the same permission being cashed again.

**Proved to be this row's own regression, by bisect rather than by argument.** The lens built a shadow package from `git show 664d3a5^:ontoloche/registry.py`, confirmed module identity, and ran the identical script: **zero** active rows answering to `zeta` before the commit, **two** after. That is the first time a lens in this register has established provenance mechanically instead of by reading a diff.

**Fixed the way the sibling call already answers the identical question.** `reinstate` on a row that is not retired returns the standing entry with `reinstate_no_op:not_retired` rather than writing; `retire` on a row that is already retired now returns the standing tombstone with **`retire_no_op:already_retired`** — the **thirty-fourth** warning value, minted in the change that closed the trip per **R3**, with its `INTERFACE.md` §5.4 row. It is also the more correct answer to the question the old behaviour was answering badly: rewriting a standing tombstone's `retire_reason`, `retired_by`, `retired_at` and `successor` is an **edit** of a provenance-bearing row, and §5.8's rule is that *a correction is a new event, never an edit*. **The path to change a successor is `reinstate` then `retire`** — two ordinary calls that each record who made them, which is exactly the path `successor_active`'s own `detail` prescribes for the neighbouring case. Pinned by **`C9-29`**.

**Two more defects of the same family were found beside it in the same round and fixed in the same commit.** The transfer wrote the **successor's own name** into the successor's own aliases when the retired row happened to carry it — `_alias_identity_breach` skips the self row when it compares, so it passed the guard, and it is the *second home for one fact* R75 is careful to avoid for `rec.name` arriving by the other route (`C9-30`). And the transfer's event was filed as **`"retired"` on the row that is alive and is the survivor**, so `provenance(successor)` told a reader the survivor had been retired — safe only by the accident that `_lifecycle_collisions` reads `detail["successor"]` and this detail has none, which is one dict key away from being read as a succession edge. One event value carrying two facts is §2.3's Cause B; it is `aliases_transferred` now (`C9-31`).

**Why the checker exited 0 — countable rather than descriptive, as it was for trip eleven.** `check_merge_guard.py` contained **zero** repeated calls on one row: every one of its `retire` / `merge_types` / `import_types` / `reinstate` fixtures opens its door exactly **once**. Row 6c's own **eighth** axis drives both alias shapes through this door on three legs *and* the two paging doubles, and still could not pose *"what if this door is opened twice?"* — **the seventh consecutive dress of *a checker only asks the questions its fixtures can pose***, and the second time in a row that the reason is a countable absence rather than a subtle one.

**A NINTH axis closes it: THE SAME CALL, TWICE.** All four collapsing callers are opened twice on every leg and the result held against `C16-06`'s whole-store invariant — every active row's `name` **and** aliases, keyed by `identity_key` so the seventh and eighth trips' lesson is inherited rather than restated. **Proved by mutation**: reverting the `retire` no-op turns the `retire` row `FAILED` on both fully-capable legs and the checker exits 1. The invariant the axis holds is the one that closes the family, and it is stated as a rule rather than as a fixture: **the write a call performs must be idempotent in the state the guard read.**

**Three MAJOR of neighbouring families in the same round, from the integrator lens, all in the READ half or in what the code does when nobody passed the optional argument** — the fourth consecutive row that sentence has been true. A `TypeRef` whose `name` carried a `#` was stored and read back as an **`InstanceRef` naming an object that never existed**, with no exception anywhere: not the permissive reading of something unrecognised that R72 was written against, but a **confident reading of the wrong thing**, refused at the write door now so the ledger stays parseable by construction (`C19-82`). `invocations(unreviewed=True)` read the family's mode **today** rather than the mode of record, so an ordinary amendment moved historical invocations into and out of the review queue with no event and no warning (`C19-84`). And `review_invocation` accepted a `namespace` it never honoured, so a queue operator scoped to one tenant drained another's (`C19-85`).

**The worker's reading, offered for countersignature rather than assumed.** *(A trip is never the worker's to judge alone; this record was written in the eleven records' shape and left for the supervisor to read against the commit.)* **(1)** the design is still not what tripped — `namespace` is untouched across all twelve and `cross_namespace_merge` still refuses on live NYC data, and every trip was caught in test, none in a real merge. **(2)** This trip is different from nine, ten and eleven in a way that matters to the `stop` question and should not be folded into the tally: those three were one defect found three times, and the required-keyword rule closed that family; **this one is a regression the row itself introduced, found within one round, by a lens that then proved the provenance by bisect.** Read one way that is the loop working exactly as constraint 7 intends; read another it says an amendments row over a *specified* surface still managed to open the kill row, which is the strongest argument yet that guard-adjacent writes need the checker extended **before** the write lands rather than beside it. **(3)** The register's standing rules gain one: *a guard evaluated once for a call that can run twice is a permission cashed twice*, and its checkable form is the ninth axis. **Recommendation to the founder: the `stop` option is put explicitly for the eighth time**, with the honest counterweight that R75's design test — which is what put a write behind this guard at all — also found and fixed a real two-row identity defect that had been live since row 4d.


**Supervisor judgment and countersignature (2026-09-01, commit `3844f92`; countersigned by the supervisor restarted in tmux that afternoon, who read the record against the code rather than against the commit message).** The record above was drafted by row 6c's worker; I concur with it, including the classification, and I verified the closing change mechanically rather than taking the record's word for it: `retire_no_op:already_retired` is emitted at `registry.py:2915` and declared in `types.py`'s `WARNING_VALUES`; `INTERFACE.md` §5.4 carries its row and the table's count now reads **thirty-four**; `C9-29`, `C9-30` and `C9-31` are claimed by name-bearing test functions in **both** `contract/test_c9_retire.py` and its `aio/` twin, which is how `test_manifest.py`'s `_TEST_NAME` census counts an id as implemented, so the three are pinned on the async leg as well as the sync one; the ninth axis stands at `check_merge_guard.py:2347` under the heading the record names. **Four things for the register.**

**(1) The BISECT is the part of this record that should outlive the trip, and it becomes a rule.** Eleven records established provenance by reading a diff and saying *this row introduced it*; this lens built a shadow package from `git show 664d3a5^:ontoloche/registry.py`, confirmed module identity, and ran the identical script to get **zero** holders before and **two** after. That is the difference between a claim and a measurement, and it matters most in exactly the case this row was in — a row that both **ships a write** and **reviews it**, where *"our own regression"* is otherwise the row grading its own homework. **Standing rule from here: when a trip is found in a round whose own row introduced the write under the guard, the round proves the provenance by bisect, not by argument.** It costs one shadow module and it removes the one judgment a worker cannot make impartially about itself.

**(2) The eighth axis was written FOR this door, in this row, and still could not pose the question — and that is the finding, not the trip.** Row 6c added axis eight specifically to drive `retire(successor=)`'s alias re-point on three legs plus both paging doubles, and it exited 0 with two live rows answering to one word. The worker's diagnosis of why is countable and correct: the file contained **zero** repeated calls on one row. So the register's lesson is no longer *"a checker only asks the questions its fixtures can pose"* — that has been said seven times and is not worth an eighth — it is one step sharper: **a fixture that opens a door once cannot test a permission; it can only test a write.** Every axis in this checker was single-call until today, which means the whole gate was, for its entire life, blind to one of the two ways a guard fails. Axis nine closes it for the four collapsing callers, and I am recording the generalisation as the register's rule so a future axis inherits it: **the write a call performs must be idempotent in the state the guard read.**

**(3) I adopt the worker's third point as a ruling, because it answers the question its own second point raises.** If an amendments row over a *specified* surface can still open the kill row, then the ordering that failed here is the one to fix: R75's write landed at `664d3a5` and the axis able to open its door twice landed at `3844f92`, one adversarial round later. **Ruling: a change that adds a writer of an identity field lands with the checker axis that can pose the new call's failure mode IN THE SAME COMMIT, or it does not land.** This is standing constraint 8 (*spec and ids in the same commit*) extended to the gate, and it is the first rule this register has written about *sequence* rather than about content. It would have caught this trip before the lens did, and its cost is one axis per identity-field writer — the project has produced four of those in twelve rows.

**(4) The suite, stated honestly, because round 1 is NOT clean and the record above does not say so.** Async three legs at this HEAD: **835 passed**, above the row's floor of 796. Ids **327 → 346**. But the full sync leg at `3844f92` came back **1 failed, 796 passed, 260 skipped** — `test_manifest.py::test_every_optional_capability_can_be_declined_alone` — and **this one is not what row 6b's D-6b-10 was.** There the same test wedged under contention and `check_capability_matrix.py` run standalone immediately afterwards exited 0; here the checker ran to completion under no contention and **reported five configurations that cannot pass the suite**: `stores_events=False` (4 failed), `stores_attributes=False` (3), `stores_attributes=False +proj` (3), `stores_invocations=False` (2) and `stores_invocation_events=False` (2). Two of those five are the ACTIONS-layer capabilities this row's own R73/`review_invocation` work touches. **[Observed — bisected, `54f7f26`]** that makes it this row's regression rather than a standing condition: at `ddf2f5e` (6b's landing, 6c's parent) the checker reports all eighteen configurations conformant, exit 0. *(This sentence read `[Inferred, not yet bisected]` when the countersignature was written; the worker's bisect confirmed the inference and found the cause — `get_closest_marker` returns ONE mark, so a stacked `@requires_capability` silently dropped all but the innermost — and it was closed in the same commit.)* It is `nonbinding` under ruling **R2** and so does not touch the conformance verdict — and it is nonetheless a red on `main` and **row 6c does not land until round 2 closes it or narrows `PACKAGE.md` §3.2 to name only what is verified**, which is the choice the checker's own message puts. Recorded here rather than in the run record because the countersignature is where this register's honesty about a row's state is supposed to live.

**Recommendation to the founder: continue — and the `stop` option is put explicitly for the eighth time, as the worker put it.** Twelve trips in test, none in a real merge; `namespace` untouched across all twelve and `cross_namespace_merge` still refusing on live NYC data. The counterweight I would add to the worker's is narrower than its own: this trip was not found because the loop got lucky but because **R75's design test put a write behind a guard and the row was then required to review that door** — the same requirement found a two-row identity defect (`C9-30`) and a provenance defect (`C9-31`) live since row 4d in the same round. A process that manufactures its own hardest test case twelve times and catches it twelve times before any user sees it is the argument against `stop`; the argument for it is unchanged and is the founder's to weigh. The class-closing question for the read side remains **Q56**, his.

## The kill-row trip — THIRTEENTH instance (2026-09-02, row 6c's second adversarial round), and it is the TWELFTH's class at the sibling caller

**What happened [Observed, row 6c].** **Five ordinary calls. No alias write, no import, no `force`, and every identity guard passing honestly at the moment it is asked.** Reproduced on sqlite, on both paging doubles and on the async mirror, with `check_merge_guard.py` exiting **0** — *including the ninth axis written one round earlier for exactly this failure mode.*

1. `alpha`, `beta` and `gamma` are `kind="predicate"` entries whose extents are `{aaa_note, bbb_memo}` — **non-empty and identical**, so §5.10's refusal **#2** passes honestly;
2. `merge_types("alpha", "beta")`. **Correctly allowed.** `beta.aliases == ('alpha',)`, and `alpha` becomes a tombstone with `successor="beta"`;
3. `ccc_draft` is registered declaring `beta` — **ordinary vocabulary growth**, the sixth trip's own second act;
4. `merge_types("alpha", "gamma")` — the operand is a **tombstone whose words have already been spent**. Returned a `MergeResult`;
5. `beta.aliases == gamma.aliases == ('alpha',)` — **two ACTIVE rows answering to one word**, which is `C16-06` and mechanism **4** — and `resolve_type("alpha")` answers **`gamma` at confidence 1.0** on a pair `merge_types` itself refuses `predicate_merge` **non-overridably under all five acknowledgements**.

**It is the twelfth trip's class at the sibling caller, and that is the whole sentence.** Trip 12's rule is *the write a call performs must be idempotent in the state the guard read*, and row 6c's round 1 made **`retire`** idempotent **per caller** (`retire_no_op:already_retired`). `merge_types` performs the identical write — `aliases = right.aliases + (left.name,) + left.aliases` — and a tombstone **keeps its words by design** (§5.8), so `left.name` and `left.aliases` are an **unconsumed permission** that `merge_types` will cash for any `right` a caller names, once per call, forever.

> **So the obligation belongs to the ROW's words, not to the caller.** `retire` got the per-caller answer; the per-row question is *who holds these words now*, and it is the one that ends the family — every future writer of a tombstone's words inherits it rather than needing its own no-op guard.

**Why the guard that exists did not fire.** §5.10's identity guards compare the two **operands**; they never ask whether the words being **moved** are already held elsewhere. `_identity_breach(left, right)` was assumed to cover the transfer and does not.

**Why the checker exited 0 — countable, for the third consecutive trip.** Two independent counts. **(1)** `merge_types` contained **zero** calls to `_alias_clash`, `_alias_holder` and `_word_rows` — the seventh trip closed `C16-06`'s invariant at four write doors (`propose_type`, `approve`, `reinstate`, `import_types`; `C4-12`, `C9-23`, `C12-12`) and **`merge_types` is the fifth door and the only one that asks nothing.** **(2)** The **ninth axis's own `merge_types` fixture repeated into the SAME target**, so its write was idempotent by coincidence and the axis reported `{}` with the defect live — while `_repeat_retire`, in the same axis, correctly used two different successors. **The asymmetry between two fixtures in one axis was the entire gap**, and it is the eighth consecutive dress of *a checker only asks the questions its fixtures can pose*.

**Provenance, by BISECT — and it splits in two, which matters for whose trip this is.** Standing rule (a) of the twelfth countersignature applied, with a shadow package at `664d3a5^` and module identity confirmed:

| variant | at `ddf2f5e` / `664d3a5^` | at HEAD |
|---|---|---|
| **A — `merge` → `merge`** (the five calls above) | **already live** — two holders, `resolve_type` at 1.0 | same |
| **B — `retire`(R75) → `merge`** | **zero holders** | **two holders** |

**Variant A predates row 6c entirely** — it is live at 6b's landing, in a door row 6c never touched. **Variant B is row 6c's own regression**, introduced by R75 at `664d3a5`, the same bisect signature as the twelfth trip one caller along. *(Verified independently by the worker as well as by the lens, against the worktree detached at `ddf2f5e`.)*

**Fixed** by giving `merge_types` the collision question at the words it writes — `(left.name, *left.aliases)` against every active holder other than the target, keyed by `identity_key`/`same_word` through the existing `_alias_clash`. **`alias_collision`, non-overridable**: `retired_operand` is an **evidence** acknowledgement about a row's `status` and this is an **identity** outcome, which is `C9-20`'s own distinction at a door nobody had applied it to — *a guard that can be acknowledged past is a warning wearing a refusal's name.* Rule U on the **look** rather than on its result (`C12-13`, `C4-12`): a scan that could not finish warns and proceeds, because refusing on a short page would ban `merge_types` on every paging backend. Pinned by **`C10-20`**, and the ninth axis's `_repeat_merge` now repeats into a **different** target, **proved by mutation**.

**Two findings of the same family beside it, NOT fixed, and raised as questions rather than taken** — both are the founder's territory under this row's scope fence, and both are recorded with their bisect evidence: **R75 minted a class of word that answers at §5.3's guarantee and that Q56's cheap half cannot see** (a transferred alias has no row of its own, so `_identity_stale` short-circuits — the retired NAME warns `identity_stale` and the word it moved does not, in one store), and **`list_types(predicate=<a transferred alias>)` answers a confident zero** for a word three other doors call one identity. **[Observed]** `resolve_type('zeta')` was `proposal / None / 0.7500` before R75 and is `existing / beta / 1.0000` after.

**Supervisor judgment and countersignature — DRAFTED BY ROW 6c'S WORKER, AWAITING COUNTERSIGNATURE.** *(A trip is never the worker's to judge alone.)* The worker's own reading, offered rather than assumed: **(1)** the design is still not what tripped — `namespace` is untouched across all thirteen and `cross_namespace_merge` still refuses on live NYC data; every trip caught in test, none in a real merge. **(2)** This trip is **two facts and the register should not merge them**: Variant A is the first trip found in a door **no row of its lineage had touched, at a commit before the row began** — the loop reaching past the diff, which the seventh countersignature first noted and which has now produced a kill-row trip rather than a finding; Variant B is 6c's own regression and is the twelfth trip's class one caller along, which says the per-caller fix was the **narrow** answer to a **per-row** obligation. **(3)** The register gains the rule that ends the family: **a tombstone's `name` and `aliases` are an unconsumed permission, and every caller that transfers them must ask who holds them now** — the per-row question, of which trip 12's `retire_no_op` is one caller's special case. **(4)** Standing rule (b) of the twelfth countersignature is vindicated and should be **widened**: R75 added a writer of `aliases`, and the axis that landed a round later still could not pose the *sibling caller's* version of the same failure — so *the axis a new writer lands with must drive every OTHER writer of that field too.* **Recommendation to the founder: the `stop` option is put explicitly for the ninth time**, with the counterweight that this row's loop has now found a defect that eleven prior rounds across four rows did not.
