# Rulings 2026-08-29 — the five questions row 3e left (Q22–Q26 → R27–R31)

Supervisor rulings under the founder's make-assumptions directive. Source: [`docs/runs/3E-RUN.md`](../runs/3E-RUN.md) §7. Each is yes/no on the worker's recommendation; the founder may reverse any. R31 is a process ruling with teeth and is flagged in the pane report.

## R27 (Q22) — `Resolution.complete=True` rarely reachable behind the five-item cap: KEEP as shipped in v0

An honest flag that rarely fires beats a flag that lies. Splitting `complete` from a `truncated` field is the same question as Phase 3's paging decision (R13/R25) — what a bounded list may claim — and is decided there, together.

## R28 (Q23) — a name-level attribute schema cannot be removed: v1 `AttributeStore` delete; §9.4 drop-and-recreate meanwhile

Accepted. It would be the package's first destructive operation on deployment configuration; it gets the same treatment `retire` got — specified with refusals, not slipped in.

## R29 (Q24) — `attr_schema_version` ambiguous without the entry's name: RECORDED, deferred

Accepted as recorded. The fix (a second `oo_type` column, store version 4) waits for a deployment with three schema generations in flight.

## R30 (Q25) — `attribute_census` linear in types-of-a-kind: ACCEPT for v0 with the stated warning

Adding a list-schemas method would silently un-implement the extension for existing third-party backends (`runtime_checkable` matches on method names) — a versioned extension does it properly at v1.

## R31 (Q26) — spec rules become executable: YES — **standing constraint 8, from the next spec row on**. *Founder-visible (process).*

**The measurement:** thirteen of 3e's twenty-one new contract ids exist only to pin claims the specifications already made; round 3 found four false prose sentences no gate could see; across three rows, not one finding of substance came from reading a diff. **The ruling:** every numbered rule in a spec section ships with either (a) a contract id that exercises it, or (b) an explicit `prose-only` tag with the reason — and `check_spec_drift.py` fails on a numbered rule with neither. Applies from row 4b's landing onward (4b maps EDGES §2.4.1, §4.3 and §4.4's rules first, since it is implementing them) and to #6 (actions spec) as it is written. This does not make the adversarial loop unnecessary; it moves the reviewers' cheapest findings into the gate so the loop spends its rounds on the expensive ones. Recorded as ROADMAP standing constraint 8.
