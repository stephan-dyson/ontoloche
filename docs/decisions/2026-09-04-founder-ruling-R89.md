# R89 — FOUNDER RULING: `stop` is not taken; the loop moves out of build rows; the dedicated identity-surface row opens

**Ruled by the founder, 2026-09-04 22:44**, on [R88](2026-09-04-7a-supervisor-ruling-R88.md)'s
eleventh put of the kill-row `stop` question. The founder's words, verbatim: **"agree with your rec."**
Recorded by the ontoloche program supervisor.

## What was recommended, and is now ruled

R88 put both columns fairly and recommended: *continue — and STOP HOUSING THE LOOP IN BUILD ROWS; open
the dedicated identity-surface row.* The founder agreed. So:

1. **`stop` is NOT taken.** The kill-row adversarial loop continues. This is the eleventh time the option
   was put and the eleventh time it was declined — and the **first** decline that rests on a row with no
   trip behind it, which is why it is the founder's ruling rather than the supervisor's recommendation.

2. **The loop's housing changes.** Every build row since 4c paid two or three adversarial rounds to the
   identity surface as a tax on its own diff. That stops. The kill row's home is now a **dedicated
   identity-surface row** with no feature and no amendment of its own — the fourteenth countersignature's
   recommendation, sequenced behind row 7a exactly as it said, now opened.

3. **Row 7a is validated as landed.** `INGEST.md` v0 ships with eight open findings named in §8.1a; the
   founder has seen it. Spec §7 gate 4 is satisfied and the 7a session closes.

## The argument the founder agreed with, in one paragraph

Row 7a demonstrated that the loop can **predict** where its next defect will live — seven pre-registered
predictions at `4f3b2eb`, seven confirmed — and, in the same breath, that prediction is not prevention:
P3 was written by the author of rule 3-19, after rule 3-19 existed, and could therefore only be confirmed.
**A loop that can predict its next defect is wasted in a row that writes the rule first and predicts
second.** In a row with no diff of its own, the prediction arrives before the rule — the only configuration
in which the loop's demonstrated capability becomes the thing the register has never had: prevention.

## What opens now — row 6d, the identity-surface row

Numbered **6d** because the fourteenth countersignature placed it *"in the actions-continuation slot the
founder ruled parallel on 2026-09-02"*, and because its surface is the five doors the actions arc built
and the guards row 6b extracted. Its brief is the supervisor's to write; its shape is fixed here:

- **No feature. No amendment. No product change of its own** except what closing a finding requires.
- **Lens set:** the **fourteen trip records** and the **eight instance-surface cells** (`I-1`…`I-8`), read
  as one register.
- **Surface:** the type half — `_word_rows`, `_alias_holder`, `_alias_clash`, `_identity_stale`,
  `_identity_closure`, and the five doors (`retire`, `merge_types`, `propose_type`, `approve`,
  `import_types`) — against the ten-axis `check_merge_guard.py` as its gate. The instance half exists only
  as a spec; the row **predicts** against it and hands those predictions to the `INGEST` build row.
- **Round 1 is pre-registered prediction, not a lens.** `6C-RUN.md` §6.7's four predictions and
  `7A-RUN.md` §6.11's method are the template: the row writes where it expects each of the twenty-two
  records' shapes to recur across the whole surface **before** any lens runs, commits that, and only then
  dispatches. That ordering is the entire point of the row.
- **Q81 and Q82 belong in its brief** (rulings R79/R80).
- **It does NOT take E22 or E2.** Those are the `INGEST` **build** row's first two obligations per
  `INGEST.md` §8.1a and stay there. The identity-surface row and the build row are different rows; opening
  one does not open the other.

## Unchanged

Kill-row trip count is **FOURTEEN**. Q56 remains the class-closing question for the read side and the
founder's. Q85–Q93 still want rulings (Q91 the founder's). Standing rules (a)–(e) stand as R83–R88 left
them. The `INGEST` build row is **not** opened by this ruling.

Next ruling number: **R90**. Next question number: **Q94**.
