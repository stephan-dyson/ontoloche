# Rulings 2026-08-30 — the eight questions row 4d left (Q61–Q68 → R61–R66; Q65, Q66 and Q68 carried to the founder's Q56 decision)

Supervisor rulings under the founder's make-assumptions directive. Source: [`docs/runs/4D-RUN.md`](../runs/4D-RUN.md) §8. Each is yes/no on the worker's recommendation; the founder may reverse any. **Three questions are not ruled here because they are the founder's Q56 decision seen from three more angles** — Q65 (a third option), Q66 (what the warning means over time) and Q68 (what the registry declines to hold) — each with its default stated. Two of the six rulings route work to the ACTIONS build row **6b**, which is already ruled (R53, and the seventh-trip countersignature) to be the review row 4c's guards did not get.

**Where the row ended:** 226 → **249 ids**, sync `596 passed`, async `631 passed`, three legs; warnings 29 → 32; `Refusal.reason` 28 → 30 (`successor_unregistered`, `successor_is_self` — both about a guard that could not be *evaluated*, not the founder's half of Q56); `check_merge_guard.py` 1 → **6 axes**; constraint 8 reaches `INTERFACE.md` (21 rules, 20 pinned, 1 tagged). Three rounds, six lenses, **31 findings, 16 BLOCKING, did not converge** (§6). Kill row: trips **seven and eight**, one defect found twice (the guards compared alias bytes, the resolver compared normalised words), countersigned in [`2026-08-29-3c-rulings-R6-R12.md`](2026-08-29-3c-rulings-R6-R12.md). **Suite floor from here: 249 ids / sync 596 / async 631.**

## R61 (Q61) — ONE `identity_stale` value in v0; the always-on case is recorded, not split. Revisited on beacon 2B's evidence.
The two facts (*the extents differ* / *cannot be known to agree*) have the same consequence for a caller — do not trust this 1.0 without looking — and splitting doubles a vocabulary for one consequence. The residual is real and named: on `indexes_membership=False` (UC1 Tenshen's declared shape, and the `sqlite_minimal` leg) **every** predicate redirect carries it permanently, and this project has twice ruled *a signal that never turns off is noise*. **Revised by:** beacon 2B's harness showing the warning fires on every Tenshen redirect — then split under R3 into `identity_stale` / `identity_unverifiable` in the same change that adds the §5.4 rows.

## R62 (Q62) — the closure cache stays; measured, not ruled. Phase 3's ingestion row re-measures at `erm2-nwe9` scale.
Round 2 measured before changing: wall clock quadrupled per doubling of predicate count (13.9× plain, 26.5× with `of=`, at 3,481 rows); one cache threaded through the call took 80 merged pairs from 109.6 ms to 22.3 ms. Recorded so the Phase 3 row that first runs `predicates()` in a loop (R58) measures again rather than assuming.

## R63 (Q63) — `consumers` / `Consumer.gate` need nothing further; the surface that was wrong (`list_types` on an alias-only identity, `C6-09`) is fixed. Recorded.
The question as first written named the wrong surface, and running it is what showed that — `Consumer.gate` already resolves the identity under R38 and queries `include_retired=True` deliberately. Kept in the register as the example of a question answered by a probe rather than a ruling.

## R64 (Q64) — the guards' written-word reading is NOT named in v0; row 6b names it with R53's extraction.
R53's reason applies verbatim (changing the guards' shape in the row that changes what they compare is how trips 2, 5, 6 happened). The mitigation that shipped is stronger than a name: round 3 proved by **mutation** that `C10-13`, `C10-14`, `C10-16`, `C10-17`, `C10-18` and `C3-14` all break if the default is flipped. 6b renames (`_written_extent`, or a required keyword) in the same change that extracts.

## Q65 — is `resolve_type` the right place for Q56's second half, or is the answer a NEW READ (*"is this identity still sound?"*)? *CARRIED TO THE FOUNDER — not ruled.*
The founder's open half of Q56 was *refuse, or lower the confidence*. Row 4d found a third shape: leave §5.3's guarantee alone and add a read a steward asks before a merge and a consumer asks before trusting a redirect — today assemblable only from two `predicates()` calls. **Default in force:** the warning that shipped; no new call (INTERFACE §1's non-goals list is load-bearing). The founder's Q56 ruling should see three options rather than two.

## R65 (Q66) — `identity_stale` stays a strict comparison and is therefore PERMANENT after the first post-merge declaration; §5.3.2's reading of the value is the ruling. *Carried to the founder as Q56 evidence.*
Weakening to containment would miss Door 1 (the sixth trip's own walk), so the comparison stays. The value means *this identity has grown apart from the equality that justified it*, not *act on this now* — §5.3.2 says so. **What this does to the founder's half of Q56:** a *refusing* read would refuse forever after the first ordinary type declared against a survivor — which is the ordinary case, not a contrivance. That is the strongest evidence on the record for the warn-only default and against refusal; lowering confidence has the same permanence problem. If the founder rules that permanence makes the warning noise, R66 names the surface that would carry the better signal.

## R66 (Q67) — `_extent(identity=True)` / `PredicateEntry` does NOT carry a staleness signal in v0; recorded against R65.
`PredicateEntry` has no `warnings` field, so this is a shape change (R3), and `resolve_type` already warns on the same fact. Taken only if the founder rules R65's permanence is noise — then `PredicateEntry.warnings` is the carrier, in a row of its own.

## Q68 — should `identity_key` ERASE non-Latin words, or should the registry REFUSE to hold what it cannot compare? *CARRIED TO THE FOUNDER — not ruled; it decides what the registry declines to serve, as Q50 does.*
The hole is closed (`same_word`: an empty key is never equal to anything, `C4-14`), but the fact remains: the registry can hold an alias it cannot compare, and every guard passes it silently. UC3's catalogue is multi-agency and not every label is Latin. **Default in force:** hold it, never compare it, warn nothing — the shipped behaviour. The alternatives are a real Unicode fold (NFKD, combining marks, per-script rules: a dependency and a decision about which scripts fold together) or `Refusal(reason=…)` on an unreadable word. **Evidence that decides it:** Phase 3's ingestion row on the multi-agency catalogue (R58–R60), which is the first consumer to hand the registry a non-Latin label at scale.

---

## What these do to the roadmap
- **Row 6b — ACTIONS build** (next, Opus): `C19` (58 planned ids), `check_spec_drift.py` pointed at `ACTIONS.md`, and **R53's guard extraction with R64's naming — whose first adversarial lens is the extracted function against all EIGHT trip records** (seventh-trip countersignature). 6b may extract and name; it may not change what the guards compare.
- Phase 3 (R58–R60) inherits R62's re-measurement and Q68's evidence obligation.
- The founder's Q56 decision now has, on the record: the default (warn, shipped), the expensive half (refuse / lower), the third option (Q65), the permanence evidence (R65), and the non-Latin question (Q68).

## The kill row's `stop` option — put to the founder for the fourth time
Eight trips in test, none in a real merge; seven and eight are one defect. Supervisor recommendation: **continue** with row 6b, because 6b is the review those guards did not get. A word of `stop` halts launches.
