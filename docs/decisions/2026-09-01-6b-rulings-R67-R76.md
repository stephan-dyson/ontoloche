# Rulings 2026-09-01 — the ten questions row 6b left (Q69–Q78 → R67–R76; Q70 and Q71 founder-visible, routed with defaults into the Q40/R44 retention decision)

Supervisor rulings under the founder's make-assumptions directive. Source: [`docs/runs/6B-RUN.md`](../runs/6B-RUN.md) §8. Each is yes/no on the worker's recommendation; the founder may reverse any. Two questions (Q70, Q71) are founder-visible because they are the **second and third time Q40's unanswered retention question has produced a defect rather than a cost** — both are routed here with defaults in force, and the retention decision itself stays the founder's under **R44** (*types, edges and invocations have one retention question and want one answer*). Five rulings route work to a new amendments row, **6c**, defined at the bottom.

**Where the row ended:** 249 → **327 ids**, sync `757 passed`, async `796 passed`, three legs (post-rename); adapter primitives 18 → **21**, store schema v5; `Refusal.reason` **30** and warnings **32**, all returned by code; `check_spec_drift.py` reads `ACTIONS.md`; `check_merge_guard.py` 6 → **7 axes** (consumer sets, proved by mutation). Kill row: trips **nine, ten and eleven — one missing-operand defect three rounds apart**, closed by the required-keyword rule and countersigned in [`2026-08-29-3c-rulings-R6-R12.md`](2026-08-29-3c-rulings-R6-R12.md). Three rounds, five lenses where the brief asked six, **did not converge** (§6). **Suite floor from here: 327 ids / sync 757 / async 796.**

## R67 (Q69) — the alias door KEEPS the skip on `indexes_membership=False`; the residual is recorded, not closed here.
The recommendation is adopted on its own arithmetic: what still guards a predicate pair there is refusal #2 (unknowable extents refuse), and the unguarded case — a non-predicate pair with differing consumer sets — is equally unguarded at `merge_types` itself on such a backend, so the door is no weaker than the call it mirrors. Refusing instead would ban `import_types` from writing any aliased row on UC1 Tenshen's own declared shape. **Closing it properly is a `Capabilities` question** (a backend reporting `predicates` independently of `indexes_membership`) and is the same question as Q78 — routed together under **R76**.

## R68 (Q70) — the ledger's size is RECORDED against Q40/R44; the paging half is R58's; nothing is built. *Founder-visible.*
There is no longer a bound to lie about — the one derivation that walked the ledger is an indexed lookup since D-6b-5/D-6b-9 — so this is back to being a *retention* question, and R44 already rules that retention gets **one answer** across types, edges and invocations. What this row adds to that decision, and why it is founder-visible: **an unbounded ledger produced a correctness defect the moment anything derived from it** (three defects, one cause, §5/D-6b-5 — 200,020 row reads for twenty returned rows, a bound reporting the wrong `outcome`). **Default in force:** no rotation, no cap, one row per action per data row, forever — the shipped behaviour. The integrator lens's ask (a number and a rotation story before wiring 222 actions) is carried to the founder's R44 decision, not answered here.

## R69 (Q71) — no count on `invocations(unreviewed=True)` in v0; routed with Q70 into the same decision. *Founder-visible.*
A `known` that meant *the size of the set* on a filtered read is precisely the ambiguity R13 declined to resolve and **R58** resolves for Phase 3 (`known` counts the page; `complete` is about the set). Taking a count here, ahead of R58's paging landing, would mint the fourth meaning of `known` this project has had to argue about. **Default in force:** `review` mode ships without a queue depth; a host that needs one maintains it outside the registry. **Why founder-visible:** this decides whether `approval_mode="review"` is *operable* by the one host that exists — a review queue nobody can size is a governance mechanism that cannot be operated, and that fact belongs beside Q70 in the R44 decision rather than buried under it.

## R70 (Q72) — rule 10-9's typo judgement is NARROWED to the namespace-scoped pool; taken in row 6c, which must re-run §10's design test.
The worker's proposed rule is adopted as the target: *a typo is an `order` naming groups no family in THIS SCOPE carries, where the scope declared any surface at all.* Getting this wrong in either direction has already cost a round each (round 1 of the spec row moved it out of the filtered pool because an empty namespace was refusing a real projection; this row found the co-tenant refusal). That is exactly why it is **not patched here**: the change is taken only in a row that re-runs §10's design test against UC3's many-publishers catalogue, with the `C19` id amended in the same change per standing constraint 8.

## R71 (Q73) — a retired edge family inside a declared blast radius WARNS at invocation, never refuses; taken in row 6c.
`edge_family_retired` already exists in §5.4 for the identical fact one layer down — no value is minted, which is INTERFACE.md §2.3's own discipline. Refusing would make a steward's ordinary retirement break every host mid-flight, the shape §2.5 refuses twice. It is a rule-table change with its `C19` id, so it belongs to 6c, not to a ruling.

## R72 (Q74) — the PARSER is taken, not the shape: a public inverse of `ref_key`, one additive function, in row 6c.
The flat identity string is the honest storage form (`InvocationRecord.inputs` is JSON, PACKAGE.md §3.3). What is missing is the read half `ACTIONS.md` §2.3 promised — *"the reference can be read without a store round trip"* — which today requires every consumer to hand-split `"beacon:edge:b_edges#e-abc123"`. 6c exports the inverse and amends `ACTIONS.md` to print it in the same change, because a surface the document does not print is how D-6b-1 happened.

## R73 (Q75) — `review_invocation` is SPECIFIED as ACTIONS.md's fifth call, adopting D-6b-3; `unknown_invocation` is minted in the same change. Row 6c.
D-6b-3's argument is accepted in full: a review is a second act by a second person at a later time, and folding it into `record_invocation` would let the actor mark their own invocation reviewed. §7's argument for declining `unknown_invocation` was explicitly conditional on *no call naming an existing invocation by id* — a fact the fifth call changes — and reusing `action_family_unknown` for a missing *invocation* is §2.3's Cause B. One value under R3, with its §5.12 row, minted only when the call is specified: both halves land together in 6c.

## R74 (Q76) — NOTHING catches a prose contradiction between two paragraphs, and that is recorded as the limit of standing constraint 8. No task.
`check_spec_drift.py` holds shapes, signatures, closed vocabularies and rule tables; §19.5 already concedes that every `Fixed:` claim is a hand-written assertion nothing derives. A gate over prose agreement would be a reviewer in a script's clothing. The register carries the limit so the next reader does not mistake constraint 8's silence for coverage.

## R75 (Q77) — `retire(successor=)`'s alias half is DECIDED BY A DESIGN TEST in row 6c, not by a patch; the supervisor's hypothesis is stated and is not a ruling.
Both halves of the observation are real: a legal retirement refused `predicate_merge` over a transfer that does not happen, and a surviving alias's resolution degrades from `existing … 1.0` to `proposal / 0.4706`. The comment says *re-pointed*, the code writes nothing — the tenth trip's *one door disagreeing with itself*, in row 4d's guard, which **R53 bars this row's lineage from re-comparing without a design test**. 6c drives the design test through real data (the shape 4c used for `amend_edge` under R37). Stated hypothesis, for the record: the guard's reading is the intended one — a successor redirect that silently drops an alias from 1.0 to 0.47 is the staleness family's known hazard — so the missing alias re-point is expected to be the defect. **The test decides; if it shows the redirect is real through `resolve_type`'s chain without any write, the guard is refusing a phantom and the guard is amended instead.**

## R76 (Q78) — the `indexes_membership=False` entity-collapse gap is RECORDED; closing it is a PACKAGE.md §3.2 `Capabilities` question, answered on the U3 precedent, on 2B's evidence.
The gap is real and is an absence, not a disagreement: refusal #1 blind, #2 and #3 inapplicable, and round 3 verified both doors agree. The missing distinction — *"this type declares nothing"* versus *"we cannot see what it declares"* — is Rule U's own, one level down, in the record shape rather than in a guard. Closing it means deciding what a `False` flag entails for `predicates` reporting, which is the same decision Q69's residual needs (**R67**) and the same shape as beacon finding U3 (`attribute_projections`), the precedent for how it is answered. Taken when 2B's harness shows the shape a real degraded backend actually has; not before.

---

## What these do to the roadmap
- **Row 6c — ACTIONS v0.1 amendments** (next, Opus): R70's narrowed rule 10-9 with §10's design test re-run; R71's warn-at-invocation; R72's `ref_key` inverse, printed in `ACTIONS.md`; R73's `review_invocation` specified + `unknown_invocation` minted (§5.12 row per R3); R75's design test on `retire(successor=)`'s alias half, with the patch following the test's verdict. Every change amends its spec and its `C19`/`C10` ids in the same commit per standing constraint 8. Suite floor 327 / 757 / 796.
- The Q40/R44 retention decision now has, on the record: R68's correctness evidence, R69's operability evidence, and R58's paging rule to build on. It stays the founder's.
- **R67 + R76 are one `Capabilities` question** waiting on 2B's degraded-backend evidence; nothing in this repo moves on it first.

## The kill row's `stop` option — put to the founder again (eleven trips)
Eleven trips in test, none in a real merge; nine, ten and eleven are one missing-operand defect three rounds apart, closed by the required-keyword rule (no caller can take a reading by accident — `C12-18` pins that there is no default). Supervisor recommendation: **continue** with row 6c — it is an amendments row over specified surfaces, and the class-closing fix pattern (required keywords over permissive defaults) is now standing. A word of `stop` halts launches.
